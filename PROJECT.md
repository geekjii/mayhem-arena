# 混乱大枪战复刻项目文档

> 项目代号：Mayhem Arena
> 文档版本：1.4
> 当前阶段：P2 完整强度战斗 AI；P1 视觉收尾已接入原版枪口/弹壳递归资源并修正十张地图帧映射，桌面/Web 逐把人工验收仍作为回归项。手柄/输入重绑定与 macOS、Windows 测试包导出按当前计划暂缓。
> 实时总进度、当前阻塞和下一对话交接统一记录在 [`STATUS.md`](STATUS.md)。

## 0. 实时状态入口

本文件负责长期目标、规则与里程碑，不再承担逐轮流水账。开始新一轮开发前先阅读 [`STATUS.md`](STATUS.md)：其中记录最近一次验证结果、模块状态、当前 P0/P1 问题、Git 状态、下一步顺序和验收条件。逆向证据仍以 [`REVERSE_ENGINEERING.md`](REVERSE_ENGINEERING.md) 为准。

## 1. 项目目标

以 4399《混乱大枪战新版》（Gun Mayhem Redux）的完整 SWF 为研究基准，复刻其 2D 横版平台射击玩法、35 FPS 节奏和本地多人对战体验。

游戏首先面向同一台电脑上的本地多人对战，重点复现以下体验：

- 两至四名玩家在小型平台地图中混战。
- 武器同时造成生命值伤害和物理击退。
- 玩家因生命值归零或掉出地图而失去一条残机。
- 地图中随机出现武器箱，玩家可以拾取并更换武器。
- 玩家可以在本地多人房间中加入最多三名电脑 AI；AI 的最终目标是复现原版多人对战的完整强度，而不是仅用于演示的简化行为。
- 操作简单，但移动、射击、闪避和站位具有足够的对抗深度。
- 命中、受伤、爆炸、血条变化和重生具有清楚而自然的反馈。

当前本地逆向验证版会临时加载从原 SWF 导出的地图、角色和武器箱，用来确认结构、比例和动画来源；这些参考资源已排除在版本控制之外。若进行公开分发，将重绘名称、角色、地图、美术、音乐和音效，仅保留经过复刻的玩法与手感。

## 2. 技术方案

| 项目 | 选择 |
| --- | --- |
| 游戏引擎 | Godot 4.7.2 Stable |
| 编程语言 | GDScript |
| 渲染模式 | Compatibility，优先保证 Windows、macOS 和网页兼容性 |
| 开发设备 | Apple Silicon MacBook Pro |
| 首要发布平台 | Windows x86-64、macOS arm64/x86-64 |
| 可选发布平台 | Web |
| 内部基础分辨率 | 1000 × 560，与 Redux 原始 SWF 一致 |
| 原作参考帧率 | 35 FPS（Gun Mayhem Redux 原始 SWF） |
| 玩法模拟频率 | 固定 35 Hz，优先复现原作节奏和手感 |
| 画面显示目标 | 固定 35 FPS，与原作保持一致，不启用物理插值 |

选择 Godot 的原因是它适合 2D 游戏、跨平台导出简单、无需付费授权，并且可以通过命令行自动运行和测试。

原作的移动、动画和武器数值很可能按照每秒 35 帧进行设计。因此，直接把原作的逐帧参数放进 60 Hz 模拟会改变加速度、阻力、射速和击退。本项目的玩法模拟与画面显示均固定为 35 Hz/FPS，不通过插值生成额外画面，以尽量保留原作的运动节奏和视觉观感。

### 2.1 浏览器中的 35 FPS

35 Hz 不会导致游戏规则或碰撞系统出错。Godot 支持自定义固定物理频率，35 Hz 也高于容易产生物理稳定性问题的极低频率范围。

浏览器通常按照显示器的 60、120 或更高刷新率调用画面更新。35 不能整除 60 或 120，因此某些原生屏幕刷新周期会重复上一帧，视觉上可能存在与原版 Flash 类似的轻微帧间节奏不均。这属于显示器刷新率与原作帧率不匹配，不是游戏逻辑错误。本项目不为消除这种现象而生成 60 FPS 中间帧。

网页版采用以下处理：

- 游戏状态始终只在固定的 35 Hz 物理帧中更新。
- 画面最多显示 35 FPS，不启用物理插值。
- 弹丸使用原作式的逐帧有限位置采样，不启用连续碰撞检测；允许高速弹丸偶尔从角色或平台边缘穿过。
- 浏览器标签页失去焦点时暂停对局，恢复时清除累计时间，避免突然补算大量物理帧。
- 分别在 Chromium、Firefox 和 Safari 中验证按键、声音和帧间节奏。

## 3. 首个可玩版本范围

第一版只验证核心手感，不制作完整内容。

### 包含

- 一个主菜单。
- 一个双人本地对战模式。
- 十张按原版舞台对齐的本地视觉验证地图，可在开局前切换；桌面研究版第一张地图使用原始动态层，Web 端使用兼容静态底图。
- 两名使用原版分层结构导出并以蓝、红主色区分的验证角色。
- 左右移动、跳跃、二段跳和从单向平台下落。
- 原版五把默认武器：Sand Hawk、Dual Cool Pistols、Angry Cow、Bling Pistol、Katana。
- 对局开始前选择默认武器，并在重生时恢复所选武器。
- 武器箱生成与拾取系统；原版 13 把箱内特殊武器均已接入战斗逻辑。
- 生命值、残机、弹药和当前武器界面。
- 子弹伤害、击退、爆炸、死亡和重生。
- 胜负判定及重新开始。
- 键盘操作；手柄输入与输入重绑定暂不纳入本阶段。

### 3.1 第二阶段菜单框架

已根据原版截图和逆向包体中的 `menu_editedit`、`PLAYER_FULL`、`PLAYER_AI`、菜单文本与跳转脚本，接入以下流程：

```text
主菜单
  └─ Custom Game
      └─ Free For All
          └─ Map Selection
              └─ Player Setup（四个玩家槽位）
                  ├─ Gun → Primary Weapon 弹窗
                  └─ Perk → Perks 弹窗
```

当前菜单框架已使用本地逆向地图贴图和玩家/武器组合贴图。四个玩家卡片、AI 标记、帽子/衣服/脸部接口、颜色/Gun/Perk 入口均已预留；Primary Weapon 和 Perks 弹窗支持鼠标点击、方向键/左右键浏览与返回，点击空白处也会安全返回玩家设置。最终 Web 已人工走通菜单到对局及 Esc 返回。

四槽位的最终交互按原版截图实现，不再把前两格永久固定、后两格仅显示为预留：

- 每个已占用槽位顶部都有 `CLEAR SLOT / 清空槽位`，点击后该卡片变为空槽位。
- 空槽位保留原位置和边框，显示 `SLOT EMPTY / 槽位为空`、玩家类型提示，以及 `HUMAN / 玩家` 和 `AI` 两个按钮。
- 选择 `HUMAN` 后生成普通玩家卡片；选择 `AI` 后生成带红色 `AI PLAYER` 标记的 AI 卡片。
- 人类和 AI 均保留名称、颜色、外观、武器和 Perk 配置；清空一个槽位不能修改其他槽位的数据。
- 至少一个槽位非空即可开始，只有四槽全空时禁止开始；开局时仅为非空槽位创建角色和 HUD，AI 自动参与，无需键盘准备。

六个原版 Perks 已接入战斗系统：No Perk、Triple Jump、No Recoil、Extra Ammo、Random Weapon at Spawn、Infinite Primary Weapon Ammo。Extra Ammo 按原版规则将可用弹药容量乘以 1.33 后取整；Random Weapon at Spawn 当前从完整的 13 把箱内武器池中随机选择。

### 当前阶段暂不包含

- 网络联机。
- 单人战役；电脑 AI 作为本地多人对战的阶段 4 正式目标单独实现。
- 四人完整平衡。
- 角色换装和解锁系统。
- 枪口火焰、弹壳、换弹和部分命中/爆炸效果的原始逐帧贴图替换；箱内武器人物持枪时间轴已经完成本地父级合成。
- 十张地图内部影片剪辑的动态装饰。
- 可公开分发的正式重绘美术、音乐和商业发布功能。

## 4. 核心规则草案

### 4.1 对局

- 玩家设置初始至少保留一个角色；实际参战人数由四个槽位的非空配置决定，允许单角色进入对局。
- 每名玩家初始拥有 5 条残机和 100 点生命值。
- 生命值降到 0，或者角色掉出地图边界，均失去一条残机。
- 重生时恢复全部生命值和默认武器，并获得短暂无敌保护。
- 残机归零后淘汰；最后仍有残机的玩家获胜。

所有数值都通过配置资源保存，后续可以直接调整，不写死在角色代码中。

### 4.2 操作

| 行为 | 玩家 1 | 玩家 2 |
| --- | --- | --- |
| 左右移动 | ← / → | A / D |
| 跳跃、二段跳 | ↑ | W |
| 下落 | ↓ | S |
| 主射击 | Z | T |
| 副射击/投掷 | X | Y |

键位允许在设置中重新绑定。四人模式优先推荐手柄，以避免普通键盘同时按下过多按键时发生硬件冲突。

### 4.3 伤害与击退

每次命中分别计算伤害和冲量：

```text
生命值 = 最大值(0, 当前生命值 - 武器伤害)
击退冲量 = 命中方向 × 武器基础击退 × 角色受击倍率
```

武器伤害和击退不是同一个数值。Redux 的普通弹丸以 `firepower` 同时派生横向击退与生命伤害；一般弹丸造成的生命伤害为 `firepower × 0.4`，但特殊武器可以使用独立规则。

首版采用原作式的粗糙街机物理，不依赖真实刚体模拟。角色由代码直接控制位置和速度，落地时使用位置回退，爆炸和命中直接改写速度。弹丸只在每个 35 Hz 逻辑帧的有限采样点进行命中判断，不使用连续碰撞检测。偶发穿模、边缘判定不精确和突然弹飞属于允许保留的原作质感，只有在问题会阻断对局时才修正。

### 4.4 原版五把默认武器

以下参数来自 Redux 原始 SWF。速度、击退和时间均为原作的每帧内部单位，移植时保持它们之间的比例，并通过 35 Hz 固定更新复现。

| 编号 | 武器 | 弹药 | 主攻击 | 副攻击 |
| --- | --- | ---: | --- | --- |
| 1 | Sand Hawk | 9 | 弹速 25、散布 2、击退 23、伤害 9.2；射击者后坐 1 | Throw Gun：把枪投出，速度 25、击退 25、伤害 10，并眩晕 10 帧 |
| 2 | Dual Cool Pistols | 24 | 第一把手枪射击：弹速 25、散布 6、击退 9.5、伤害 3.8；后坐 0.4 | Shoot Gun #2：第二把手枪独立射击，使用相同弹丸参数和共享弹药 |
| 3 | Angry Cow | 6 | 普通射击：弹速 35、散布 2、击退 26.5、伤害 10.6；后坐 2 | Aimed Shot：弹速 47、无散布、击退 37、伤害 14.8，蓄力/动作时间更长 |
| 4 | Bling Pistol | 14 | 弹速 25、散布 2、击退 19、伤害 7.6；射击者后坐 1 | Make It Rain：生成撒钱效果，同时每次执行消耗自身 5 点生命值 |
| 5 | Katana | 无限 | 近战连击，每次伤害 20，包含短距离突进和约 2 帧命中停顿 | Uppercut：伤害 20、横向击退约 15～25、向上速度 10，将目标挑飞 |

复刻顺序为：行为和帧时序、伤害与击退、弹药和副攻击、反馈动画、原版视觉验证、可公开分发的重绘外观。不会直接打包或发布原 SWF 中的美术与声音资源。

### 4.5 箱内特殊武器

箱内武器不会出现在开局选择界面，弹药用尽后自动丢弃，并恢复玩家本局选择的默认武器。当前已按原版控制器接入 13 把特殊武器：

| 武器 | 弹药 | 主攻击 | 副攻击 |
| --- | ---: | --- | --- |
| Shotgun | 6 | 5 发散射弹 | 8 发 BOOM 散射，消耗 2 发弹药 |
| M4 | 30 | 单发步枪弹 | 5 发散射，消耗 4 发弹药 |
| Homing Missile | 3 | 自动追踪导弹 | 非追踪分裂弹：直飞一段后分为直线、上 45°、下 45°三发 |
| AK-47 | 30 | 自动步枪弹 | 枪托近战 |
| Baseball Bat | 5 | 高击退球棒 | 近战命中失败时投出棒球 |
| Bow | 10 | 带轻微下落的单发箭矢 | 三支带轻微下落的箭齐射，消耗 3 发弹药 |
| Sniper | 5 | 白线瞄准后射击；短按也有约 0.3 秒前摇 | 进入人物与枪支隐身；被命中或进行攻击后显形 |
| MP5K | 40 | 高速自动射击 | 三发点射，消耗 3 发弹药 |
| UZI | 45 | 高速机枪 | 宽散布射击 |
| Mini Gun | 150 | 高速连续射击 | 同款连续射击 |
| Umbrella | 100 | 雨伞近战 | 长按持续撑开并阻挡正面攻击，松开收伞 |
| Throwing Knife | 10 | 近战刺击 | 投掷飞刀 |
| Bomb | 5 | 碰人即爆；落地弹跳并逐次减速，停下约 0.1 秒后爆炸 | 碰人即爆；落地不弹跳，停下约 0.1 秒后爆炸 |

炸弹范围会同时影响敌我双方，包括发射者自身；棒球、弓箭、飞刀和雨伞则分别保留原版的投射、近战与格挡差异，以维持粗糙街机手感。

## 5. 血条与命中反馈

Redux 的血条并没有使用额外的拖尾血条，而是让显示长度每帧追赶真实生命值：

```text
显示血量 += (真实血量 - 显示血量) / 3
```

在固定 35 FPS 下，这会形成快速但不突兀的指数缓动：第一帧完成约三分之一变化，三帧完成约 70%，八帧完成约 96%。

同时复刻以下原版反馈：

- 生命值低于或等于 25 时显示低血量警示层。
- 受伤量会增加 HUD 的震动强度，震动强度每帧衰减。
- 血量归零时按原作同时生成白色扩张闪光、15 帧冲击波、5 组爆裂粒子、5 组烟尘、4 块身体碎片和 `KABOOM!` 字样，并触发 15 帧整屏震动。
- 死亡爆炸继承角色当时的横向速度，使碎片轨迹与被击飞方向自然衔接；随后扣除残机，血条继续平滑收缩至零。
- 眩晕期间每隔 7 帧生成一个原版黄色问号，随机漂移，先放大至 200%～300% 再逐帧缩小。
- 重生恢复原作的屏幕外起点：角色在 `Y=-500`、随机 `X=250～750` 处恢复并自然落回场地；真实血量恢复到 100，显示血条用同一公式平滑追上。
- 命中伤害大于 5 时从两条原版命中声音中随机播放；爆炸死亡与掉出场外分别使用四条独立的原版声音池。
- 五把默认武器的开火和挥刀声音绑定到原始动作帧，而不是按键按下瞬间；例如 Sand Hawk、Dual Cool Pistols、Angry Cow 和 Bling Pistol 在第 2 帧发声，Katana 在挥砍动作的第 5、16 或 34 帧发声。
- 默认手枪换弹不再统一使用 45 帧：Sand Hawk / Bling Pistol 为 55 帧、Dual Cool Pistols 为 56 帧、Angry Cow 为 60 帧；弹匣、套筒和转轮声音绑定到原脚本中的相对帧。
- 玩家以大于 2 的纵向速度落地时，从三条原版落地声音中随机播放，并同时生成 5 个与当前地图配色对应的原版尘土粒子。
- 武器箱出现时生成 15 帧小冲击波；拾取时播放原版 `pump2`，并抛出 4 块大碎片和 2 块小碎片。选择地图、武器和准备状态时使用原版按钮声音。
- 碰撞和死亡判定读取真实生命值，不读取正在缓动的显示长度。

## 6. 项目结构

```text
MayhemArena/
├── project.godot
├── assets/
│   ├── audio/
│   ├── fonts/
│   ├── sprites/
│   └── vfx/
├── data/
│   ├── characters/
│   ├── maps/
│   └── weapons/
├── scenes/
│   ├── game/
│   ├── maps/
│   ├── players/
│   ├── ui/
│   └── weapons/
├── scripts/
│   ├── game/
│   ├── players/
│   ├── systems/
│   ├── ui/
│   └── weapons/
└── tests/
```

主要模块职责：

| 模块 | 职责 |
| --- | --- |
| MatchManager | 对局状态、计时、淘汰和胜负判定 |
| PlayerController | 输入、移动、跳跃和角色状态 |
| HealthComponent | 生命值、受伤、死亡事件 |
| WeaponController | 持有武器、射击、弹药和换枪 |
| Projectile | 飞行、碰撞、伤害和击退 |
| SpawnManager | 玩家重生、武器箱生成 |
| PlayerHUD | 血条、拖尾、残机、弹药和武器显示 |
| CameraController | 多人取景、缩放、震动和边界控制 |

各模块通过信号传递事件，避免界面、武器和角色代码互相直接依赖。

## 7. 开发阶段

### 阶段 0：工具与骨架

- 下载并固定 Godot 版本。
- 创建项目结构、输入映射和测试入口。
- 配置 macOS、Windows 和 Web 导出预设。

### 阶段 1：灰盒双人原型

- 完成移动、跳跃、平台碰撞和镜头。
- 完成两名本地玩家输入。
- 使用简单矩形和圆形作为临时画面。

### 阶段 2：战斗循环

- 按原版帧时序完成五把默认武器、弹药、主攻击、副攻击、伤害和击退。
- 完成默认武器选择；建立武器箱与后续拾取武器共用的配置结构。
- 完成血条、残机、死亡、重生和胜负判定。
- 调整移动与武器参数，使一局可以稳定完成。

### 阶段 3：反馈与手感

- 加入角色动画、命中特效、粒子、音效和镜头反馈；原版死亡爆炸、眩晕粒子、落地尘土、武器箱碎片、基础战斗/换弹/界面声音和整屏震动已经接入。
- 改善血条动画和界面可读性。
- 进行不同键盘及手柄测试。

### 阶段 4：扩展内容

- 扩展到四人。
- 增加地图、武器和模式。
- 实现原版多人自定义对战中的完整强度电脑 AI。AI 默认按 Hard/无弱化能力设计，共用一套完整行为系统；若以后增加简单和中等难度，只通过反应延迟、瞄准误差和决策概率进行降级。
- 完整 AI 包括：目标选择、平台移动、边缘判断、跳跃与掉落救场、瞄准、主副攻击、弹药与换弹处理、武器箱争抢、弹道和爆炸躲避，以及队伍/模式目标判断。
- 替换为原创正式美术。

## 8. 首个版本验收标准

满足以下条件即视为首个可玩版本完成：

1. 两名玩家可以在同一台电脑上完成一整局对战。
2. 移动、跳跃、射击、受击和重生过程中没有阻断游戏的错误。
3. 五把原版默认武器均可选择，主攻击、副攻击、弹药、伤害和击退行为与参考版本一致。
4. 血条按照原版每帧三分之一差值的公式更新，且显示结果始终与真实生命值一致。
5. 掉出地图和生命值归零都会正确扣除残机。
6. 一方残机归零后能正确显示胜者并重新开始。
7. 项目可以在 macOS 上运行；Windows 测试包导出按当前计划暂缓，不作为本阶段阻塞项。

### 8.1 完整强度 AI 验收标准

1. 玩家设置中的任意槽位都可以清空并重新选择为人类或 AI；AI 开局后无需键盘输入即可独立行动。
2. AI 能在现有地图平台间寻找可行路径，避免无意义走出边缘，并在被击飞后主动跳跃尝试返回地图。
3. AI 能根据敌人方向、距离和当前武器选择主攻击或副攻击，并正确遵守射速、弹药、换弹和近战范围。
4. AI 会主动争抢武器箱，并对临近弹丸、爆炸和危险站位作出回避或换位判断。
5. Free For All 中 AI 能持续选择有效敌人；团队和占点模式接入后，AI 能遵守队伍关系和模式目标。
6. 一名人类玩家与三名 AI 能在固定 35 Hz/35 FPS 下完成整局，不出现持续卡住、输入串线或反复无意义自杀。
7. 多人自定义对战默认使用完整强度 AI，体验目标对应原版 Hard/无弱化能力；简单和中等档不是当前完成条件。

槽位界面验收同时要求：四个槽位包括第一个槽位都能自由清空；清空后出现 `HUMAN` 和 `AI` 两个选择按钮；选择后在原位置恢复对应完整卡片；AI 卡片具有原版式红色标记；槽位增删不会污染其他槽位配置。任意一个槽位非空时即可开始并只生成该槽位对应的角色与 HUD，四槽全空时才禁止开始。

### 8.2 完整强度 AI 实现边界

- AI 通过与人类相同的控制指令驱动角色，只能发出左右、跳跃、下落、主攻击和副攻击，不允许直接改坐标、生命、弹药或命中结果。
- 使用 `MapCatalog` 现有平台矩形构建轻量平台图，支持步行、跳跃和下落连接；不引入会改变单向平台与粗糙物理手感的通用导航系统。
- AI 决策优先级为：掉落救场与立即危险回避 → 有价值武器箱 → 有效敌人追击与攻击 → 安全巡航。
- 武器策略使用现有 18 把武器配置和攻击类型，按距离、弹药、射速及主副攻击特性决策，不建立绕过武器系统的 AI 专用伤害路径。
- 本阶段完成 Free For All 的一名人类加三名 AI，并允许全 AI 配置用于自动测试；为团队和占点目标预留接口，但 Team Deathmatch、Domination 不作为本阶段验收条件。
- 决策和动作保持固定 35 Hz。允许原版风格的小概率失误和粗糙动作，但不接受长期卡住、持续自杀或完全无法回场。

## 9. 测试策略

- 对生命值、残机、伤害和武器配置进行自动化测试。
- 对玩家输入、弹丸碰撞和对局流程进行场景测试。
- 每个阶段生成可运行版本，人工检查移动和射击手感。
- 测试不把偶发的高速弹丸穿模和边缘碰撞偏差视为失败，除非它们频繁发生或破坏基本对局。
- Windows 版本最终在真实 Windows 设备上验证键盘、手柄、全屏、声音和存档。

## 10. 风险与处理

| 风险 | 处理方式 |
| --- | --- |
| 四人同键盘按键冲突 | 支持重绑定并优先推荐手柄 |
| 街机手感难以复现 | 将全部物理参数配置化，制作专用调试场景 |
| 不同帧率导致结果不同 | 在固定物理帧中更新移动、伤害和弹丸 |
| 35 FPS 与显示器刷新率不整除 | 接受原版式重复帧，不插值；保证游戏逻辑固定为 35 Hz |
| 粗糙碰撞造成偶发穿模 | 作为原作质感保留；仅修复阻断对局或高频出现的问题 |
| 网页标签页被后台限速 | 失焦暂停，恢复时清除累计时间 |
| 网页版多键输入受浏览器影响 | 单独建立 Web 输入测试清单 |
| 与原作素材或名称过于相似 | 使用原创名称、美术、声音和地图设计 |

## 11. 当前下一步

> 本节保留历史里程碑。当前实际优先级以 [`STATUS.md`](STATUS.md) 为唯一实时依据。

### 11.1 当前活动任务

1. **已完成：**中文字体、菜单路径、人物透明色块和基础 Web 对局路径已经通过自动与人工验收。
2. **已完成接入：**通过 FFDec 和 `PLAYER_FULL` 父级时间轴提取 13 套箱内武器透明合成动画，共 834 帧；代码按每把武器的原始主/副攻击区间播放，并支持 P2 主色换色。
3. **已完成接入：**FFDec 递归导出的原版枪口火焰、两类弹壳已接入；本轮 Web 包已在全部源码工作完成后统一更新，桌面/Web 逐把视觉检查继续保留为回归项。
4. **当前 P2 规划：**先按原版截图实现四槽位的 `CLEAR SLOT → HUMAN / AI` 状态流转，再完成统一控制接口；随后依次实现平台导航、移动救场、完整武器使用、战术决策与整局回归。本项目前只更新规划，不开始修改游戏代码。能力与验收标准见 8.1–8.2 节。
5. **后续视觉增强：**继续提取原始枪口、弹壳、换弹、命中/爆炸效果和十张地图帧内部的嵌套动态装饰；主菜单 AI 动态背景继续保持静态占位。注意 `scene1/2/3` 的 1–10 帧分别对应地图 1–10，并非第一张地图的动画序列；详细接入步骤与验收标准见 `STATUS.md` 的“视觉专项执行指导”。

### 11.2 历史里程碑

1. **已完成：**下载并验证官方 Godot 4.7.2 Universal 编辑器，可在 Apple Silicon 上原生运行。
2. **已完成：**创建工程骨架，锁定 1000 × 560、35 Hz 物理和 35 FPS 显示，并建立双人输入映射。
3. **已完成：**制作灰盒地图、两个可移动角色、原版式平台碰撞、生命值、残机和胜负流程。
4. **已完成初版：**五把默认武器均具备主攻击和副攻击，使用从 SWF 核对的弹药、伤害、击退及弹速参数。
5. **按当前手感定稿：**不再逐帧校准五把武器，后续只修复影响正常对局的问题。
6. **已完成：**实现正式的对局前双人武器选择、准备和返回选人流程，替代原型调试快捷键。
7. **已完成骨架：**武器箱会按 35 Hz 计时落入平台；角色触碰后更换武器、补满对应弹药并显示拾取提示，重生仍恢复对局前所选武器。
8. **已完成：**将原版 13 把箱内特殊武器全部接入武器箱与随机出生池，覆盖散射、连射、追踪、近战、弓箭、炸弹、棒球和雨伞格挡等主/副攻击逻辑，并按原版容量与特殊攻击消耗实现。
9. **已完成战斗反馈初版：**13 把武器均有独立持枪显示、枪口反馈、弹道/爆炸/近战命中反馈；枪口火焰和两类弹壳已改为优先使用 FFDec 递归导出的透明资源，缺资源仍可回退程序化绘制。
10. **已撤回：**用户未认可上一轮原创暖色工业荒漠美术；该素材不再参与项目运行。
11. **已完成逆向基线：**确认 4399 游戏 ID 149032 的完整 SWF、1000 × 560 舞台、35 FPS、十张地图层、玩家组合结构、五把默认武器符号和武器箱符号；详细记录见 `REVERSE_ENGINEERING.md`。
12. **已完成视觉验证：**接入原版第 1 张地图主要美术层与 7 个碰撞顶面，接入五把默认武器的玩家组合图和黄色问号箱，并将 HUD 调整为原版白底、绿条、残机心形的构图。
13. **已完成攻击动作：**从五个原版武器控制器拆出主、副攻击时间轴，共 179 帧，按 35 Hz 原速播放；动作前预装贴图，并保留原作速度驱动的身体起伏与持枪手滞后思路。
14. **已完成地图验证层：**接入全部十张原版地图的静态美术、`scene3` 碰撞布局和双人出生点；已核对 `scene1/2/3` 的 1–10 帧是地图选择帧而非动画帧，并改为按 `selected_map` 固定取三层。递归扫描未发现选中帧内的多帧嵌套装饰，Web 继续使用不透明底图回退。
15. **已完成收尾：**死亡爆炸、命中反馈、受击闪红、重生下落、重生保护、换弹中断、原版递归枪口火焰与两类弹壳反馈均已接入；部分命中/爆炸画面仍保留兼容层。
16. **已完成：**加入 Web 导出预设，并在本地 HTTP 服务中验证 HTML、WASM、JS 和 PCK 可以正常加载；手柄/输入重绑定，以及 macOS、Windows 测试包导出按计划暂缓。
17. **当前阶段：**第二阶段优先优化进入对局前的菜单界面；已完成地图预览、双玩家武器卡、准备状态、控制提示、中文文案/切换入口和浏览器入口。已接入项目内 CJK 字体并通过中文页面验证。
18. **本轮收尾：**完成主/副攻击与弹药消耗审计，修正散弹副攻 2 发、M4 类副攻 4 发、弓箭三连射 3 发和 MP5K 点射 3 发的原版消耗；菜单补充鼠标悬停反馈、统一键盘返回提示和中英文切换入口。
19. **本轮继续推进：**13 把箱内武器改为独立角色持枪显示并加入 12 帧程序化攻击动作；补充卡片武器预览、空白处安全返回、静止鼠标悬停脉冲和完整中文菜单文案。中文文案为有效 UTF-8，但字体问题另见 BUG-003。
20. **贴图收尾处理：**对逆向角色贴图和分层武器贴图增加近透明像素清理与最近邻缩放，降低导出色块和放大插值造成的边缘伪影；残余素材问题继续保留在 BUG-002 供人工复核。
21. **菜单路径修复：**玩家设置页底部的 `X BACK` 现在返回地图选择，与界面提示和原版两级菜单路径一致；准备状态仍由 P1 `Z`、P2 `T` 控制。
22. **地图层修复：**校正 `scene1/2/3` 资源的适用范围为各自的地图编号，停止错误的 10 帧循环混层；十张地图三层逐图资源验收已加入自动测试。
23. **P1 战斗反馈收口：**按逆向脚本中的 `fx_muzzleflash`、`fx_shell`、`fx_shell2`、`fx_shrapnel` 和 `fx_text` 触发点接入逐武器枪口火焰、两类弹壳、散射碎片、命中/爆炸文字和拾取反馈；枪口/弹壳优先走递归导出 PNG，缺资源时回退程序化兼容层。
24. **P1 父级动画接入：**重新扫描完整 SWF 后确认其余 12 把箱内武器控制器并非缺失；通过 FFDec 将全部 13 把武器递归注入 `PLAYER_FULL` 父级时间轴，导出并接入 834 帧透明人物持枪合成动画，统一脚底锚点并支持玩家主色换色；本轮完成一次全局 Web 更新。

## 12. 已知 Bug

### BUG-001：人物贴图背后出现未抠除色块

- **现象：**人物后方出现矩形或色块背景，观感类似角色贴图没有正确抠除透明背景。
- **影响范围：**进入游戏前的玩家武器预览，以及可能使用同一批人物组合贴图的对局内角色显示。
- **初步判断：**导出的角色 PNG 可能保留了原始舞台背景像素，或导入时透明通道 / 色键处理不正确。
- **状态：**已加入运行时透明清理和像素风最近邻滤镜；本轮针对玩家主色背板增加大连通色块清理，并加入自动像素断言。源码和统一 Web 浏览器预览均通过。

### BUG-002：部分贴图与战斗动画的像素级精修

- **现象：**部分人物/武器预览可能出现矩形色块或透明通道不一致；箱内武器和部分战斗反馈仍使用程序化占位效果，未完全还原原版枪口火焰、弹壳、换弹和地图动态装饰。
- **复现：**进入 Web 版 → Player Setup → 选择不同枪械 → 开始对局，分别测试每把枪的普通攻击和特殊攻击，观察角色贴图、枪口、弹壳、命中和爆炸动画。
- **影响范围：**玩家设置预览、默认武器与箱内武器的战斗动画、命中/爆炸反馈。
- **初步判断：**13 把箱内武器的战斗逻辑和父级人物持枪时间轴已经迁移；剩余差距主要是原始弹丸、枪口、弹壳、换弹、部分命中/爆炸贴图和地图影片剪辑。
- **状态：**功能性缺口已关闭。13 套父级合成动画、枪口火焰、两类弹壳、特殊弹丸辨识、线状子弹、命中与爆炸兼容效果均已接入；剩余仅是换弹人物画面、原始 Shape 逐像素替换和桌面/Web 逐把人工对照，不阻塞 AI 主线。

### BUG-003：切换中文后全部文字显示为缺字占位或乱码

- **现象：**进入 `Controls & Settings` 切换为中文后，中文菜单无法正常显示。
- **影响范围：**主菜单、设置、模式选择、地图选择、玩家设置、Gun/Perk 弹窗及所有使用中文文案的界面。
- **已确认原因：**源码是有效 UTF-8；原先项目没有 `.ttf` / `.otf` 字体资源，菜单和 HUD 使用 `ThemeDB.fallback_font`。
- **修复结果：**已加入 Source Han Sans CN Heavy 与许可说明，建立统一字体入口；“中文混乱大枪战控制设置返回”等必需字符自动断言通过，Web 页面显示正常。
- **状态：**✅ 已修复；后续仅观察不同浏览器的排版细节。

### BUG-004：打击特效颜色错误

- **现象：**战斗中的普通打击火花、碎片等反馈颜色与原版不一致，视觉冲击力不足；先前记录的“应改为深灰或黑色”判断有误。
- **影响范围：**枪弹、近战及可能复用同一命中路径的特殊武器反馈。
- **修复要求：**参考原作使用红色、橙色等醒目的暖色打击反馈；从原 SWF 核对实际填充色、渐变、描边和透明度，不直接沿用攻击者的玩家主题色，也不凭印象固定单一色值。不得改变现有命中判定、伤害、击退、粒子数量和生命周期。
- **验收标准：**打击反馈在浅色和深色地图上都清晰醒目，整体呈现原作式红/橙暖色冲击感；普通枪击、近战及特殊武器命中逐项对照原作。
- **状态：**✅ 已修复；命中反馈改为醒目的红橙暖色，且不再继承玩家主题色。原始渐变的逐像素对照可在美术精修阶段继续。

### BUG-005：特殊武器静态贴图错误

- **现象：**13 把箱内特殊武器虽然已经接入人物攻击时间轴，但其静态展示贴图仍有错误或与当前武器不对应。
- **影响范围：**玩家设置中的武器卡片、武器箱/选择预览，以及角色未攻击时的静止持枪画面。
- **修复要求：**为每把特殊武器单独核对静态符号、人物手部组合、注册点、朝向和缩放；静态帧与主/副攻击动画分别验收，不能因为攻击动画存在就判定静态贴图完成。
- **验收标准：**13 把特殊武器逐把检查，菜单预览、静止持枪和首次攻击衔接均对应同一武器，无错图、跳位或错误锚点。
- **状态：**✅ 已修复；角色静止持枪和 HUD 预览统一读取该特殊武器的父级合成首帧，不再显示错误的通用枪图。

### BUG-006：武器箱过早闪烁

- **现象：**武器箱从屏幕上方开始掉落时就播放闪烁画面，正常存在阶段也持续闪烁。
- **正确行为：**武器箱掉落、落地和大部分存续时间保持稳定显示，仅在即将自动消失的最后阶段闪烁提醒。
- **修复要求：**从原版脚本核对箱体总寿命、开始闪烁的剩余帧数和闪烁频率；将下落/静止动画与临期闪烁分开，不能从 `age_frames == 0` 就循环闪烁序列。
- **验收标准：**箱体下落和落地后先稳定显示，达到临期阈值才闪烁，拾取前保持可见；无人拾取时在原定寿命结束后消失并进入下一次生成计时。
- **状态：**✅ 已修复；原脚本显示箱体在 `time >= 250` 才开始播放 8 帧警告时间轴，当前实现保持前 250 帧稳定、最后 8 帧警告后消失。

### BUG-007：特殊武器动画播放时角色位置出现色块

- **现象：**角色播放部分箱内特殊武器的主攻击或副攻击动画时，人物所在位置会出现矩形色块或不应存在的背景色。
- **影响范围：**13 套从 `PLAYER_FULL` 父级合成的特殊武器人物动画，包括第二玩家换色后的画面。
- **修复要求：**逐帧检查源 PNG 的 alpha、边缘 RGB、人物主色背板和大连通色块；同时检查 `recolor_composite_player()` 后的运行时纹理，不能只验证资源可以加载。
- **验收标准：**13 把特殊武器的主/副攻击从第一帧到最后一帧均无矩形底色、人物色块或透明边缘闪现；至少覆盖两种玩家颜色和左右朝向。
- **状态：**✅ 已修复；运行时清除了低透明边缘和人物窗口中的饱和导出蓝色背板，并加入合成帧像素断言。

### BUG-008：多种特殊武器的子弹效果不正确

- **现象：**多种特殊武器虽然具备命中逻辑，但弹丸外观、飞行反馈或尾迹与原版不一致。
- **影响范围：**箭、普通/玩笑追踪弹、飞刀、棒球、炸弹、投出的枪和火箭，以及散弹类特殊弹丸。
- **修复要求：**分别追踪原版 `p_arrow`（489）、`p_baseball`（490）、`p_bomb`（491）、`p_gun`（492）、`p_homing`（495）、`p_homing_jokes`（498）、`p_knife`（501）及其 `fx_p_trail`（153）依赖；核对贴图、注册点、缩放、旋转、尾迹生成间隔、重力/转向和消失条件。视觉修复不得擅自改变已核对的伤害与碰撞逻辑。
- **验收标准：**按特殊武器逐把录制主/副攻击，对照原版确认出生位置、朝向、轨迹、尾迹、命中和消失画面；每种弹丸类型至少有一项自动资源/生命周期测试。
- **状态：**✅ 已修复功能表现；八类特殊弹丸均有独立的外形、方向、旋转或尾迹表达，行为仍使用已核对参数。若追求像素级一致，后续可把程序化轮廓逐项替换为原始 Shape。

### BUG-009：普通子弹没有正确显示原版线状特效

- **现象：**实机观察中普通枪弹没有清晰可见的原版线状弹道；当前 `projectile.gd` 的程序化圆点和短线不能视为原版效果已经完成。
- **原版待验证点：**原版普通弹使用 `BULLET`（符号 487），散弹使用 `BULLET_shell`（488）。已读脚本显示弹丸出生时 `_alpha = 0`，到第 2 个逻辑帧才显示；普通弹在 `firepower <= 15` 时将 `_xscale` 设为 70，`firepower > 40` 时逐帧趋近 500；散弹在 `firepower >= 30` 时趋近 350，同时每帧缩短 16。由此可确认线长/横向缩放与 `firepower` 分档有关，而 `firepower` 同时用于击退，但仍需结合导出的 Shape 和实际武器脚本确认，不能简单写成连续的“击退值等于线长”。
- **修复要求：**递归导出 487/488 的原始 Shape 或在 Godot 中按原形状重建，保留第 2 帧显现、旋转、缩放分档和散弹逐帧缩短；核对五把默认枪及 AK、M4、MP5K、UZI、Mini、Shotgun、Sniper 的调用参数。
- **验收标准：**所有使用 `BULLET`/`BULLET_shell` 的武器都有清晰线状弹丸；弱火力、普通火力、狙击高火力和散弹长度变化符合原脚本，左右射击、散布和 35 Hz 显隐时序正确。
- **状态：**✅ 已修复；Godot 已按 487/488 脚本实现第 2 帧显现、火力分档长度、高火力伸长和散弹逐帧缩短，左右方向与速度对齐。

## 13. 当前 AI 实现进度

- **AI-1 已完成：**四个槽位均可独立清空并重新添加 HUMAN 或 AI；任意一个非空槽位即可开始，只有全空禁用开始。
- **AI-2 已完成：**十张地图均由现有平台矩形生成跳跃/下落连接图，AI 可查询当前平台、目标平台和下一平台。
- **AI-3 已完成：**AI 会沿平台图追击，在无计划离开平台时反向刹车，跨平台时主动跳跃；被击飞到左右场外或低处时优先朝场内移动并按冷却使用剩余跳跃。
- **下一步 AI-4：**根据 18 把武器的类型、有效距离、射速、弹药、主副攻击与换弹状态建立完整武器决策。太刀、棒球棍、雨伞刺击、飞刀刺击和 AK 枪托共用一套由攻击 `range/min_y/max_y` 驱动的近战接敌控制器；各武器只配置攻击选择与距离，不复制整套 AI。
- **已记录 AI 行为 Bug：**当前 AI 跳跃过于活跃。后续将跳跃限制为跨平台、越障、高差追击和击飞救场等明确场景，并增加落地/路线状态约束，避免冷却一结束就再次起跳。
- **已记录 Triple Jump Bug：**AI 当前没有空中续跳规划。后续必须读取 `max_jump_count()` 和 `jumps_remaining`，依据预计落点、竖直速度、高差及离场风险使用第二、第三跳；第三跳优先保留给高差路线和救场，不能机械地连续耗尽。
- **AI-5 抢箱规则：**每次生成武器箱时，每名 AI 独立进行一次 60% 概率判定；命中后优先前往武器箱，直到箱子被拾取、消失或当前路线过于危险。不得逐帧重抽概率。

## 14. 2026-09-09 实机验收新增规则

这些条目是用户确认的最终玩法目标，不冒充已从 SWF 验证的原版结论：

1. 雨伞特殊攻击允许长按，按住期间持续撑开并格挡正面攻击。
2. 炸弹接触人物立即爆炸，接触平台不立即爆炸。普通炸弹落地弹跳并逐次衰减至停止；特殊炸弹落地不弹跳；停止后等待 4 个逻辑帧（约 0.114 秒）爆炸。初速度、反弹系数和停止阈值作为集中可调参数。
3. 降低小平台及相邻小平台的高度差，标准角色一段跳即可登上；保持现有角色跳跃力度。
4. 普通线状子弹增加轻量发光层，但不改变线长和生命周期。
5. Sniper 普通攻击使用白色瞄准线：按住约 11 帧逐渐变清晰，松开射击；短按也要完成全部前摇。瞄准完成后不能水平移动，但左右输入仍可改变朝向，并允许上下跳跃。特殊攻击进入人物与枪支完全不可见的隐身状态，被命中或主动攻击后显形。
6. Angry Cow 特殊攻击使用两条白色瞄准线，从中心线上下各 30° 在约 18 帧内合拢。短按完成前摇后自动射击；长按完成后可带着瞄准线自由移动，松开射击。
7. 弓箭弹丸加入轻微重力，所有箭在飞行中小幅下落。
8. Homing Missile 特殊攻击改为无追踪的分裂弹：先直线前进一段距离，再生成沿原方向、上偏 45°和下偏 45°的三发弹丸。
