# 《混乱大枪战新版》逆向记录

> 目标版本：4399 游戏 ID `149032`，页面标题《混乱大枪战新版》
> 用途：本地研究、玩法复刻和视觉比对；原始 SWF 与导出素材不纳入版本控制，也不作为可公开分发的项目资源。
> 当前工程状态、故障和下一对话接力说明见 [`STATUS.md`](STATUS.md)；本文件只记录可追溯的逆向证据与接入映射。

## 1. 已确认的原始文件

4399 游戏页在脚本中给出的相对路径是：

```text
/upload_swf/ftp14/wangc/20141217/1.swf
```

配合 `serversxiao.js` 中的资源服务器，完整文件地址为：

```text
https://sxiao.4399.com/4399swf/upload_swf/ftp14/wangc/20141217/1.swf
```

服务器会检查来源页。没有带游戏页 Referer 时，得到的是约 23 KB 的 4399 跳转壳；带
`https://www.4399.com/flash/149032_4.htm` 作为 Referer 后，得到完整游戏文件。

完整文件的本地校验信息：

| 项目 | 结果 |
| --- | --- |
| 文件 | `reference/original/4399_gun_mayhem_redux_full.swf` |
| 压缩文件大小 | 7,267,386 bytes |
| SHA-256 | `0d1207955c9706299e9131a753c21b1f4a976773056c43000c5e3e540b487f05` |
| SWF 版本 | 9 |
| 压缩方式 | ZLIB |
| 解压后声明大小 | 7,846,279 bytes |
| 舞台尺寸 | 1000 × 560 |
| 时间轴帧率 | 35 FPS |
| 主时间轴帧数 | 10 |

## 2. VS Code 扩展的实际作用

本机扩展位置：

```text
/Users/greenshan/.vscode/extensions/dsy4567.4399-on-vscode-0.3.7
```

扩展不是另一个游戏版本，也没有内置《混乱大枪战新版》素材库。其流程是：

1. 根据输入的 4399 游戏 ID 打开对应页面。
2. 从页面源码读取 `_strGamePath`。
3. 读取 `serversxiao.js` 并拼出资源服务器地址。
4. 下载同一份 SWF，通过本地 HTTP 代理提供给 WebView。
5. 使用 Ruffle 在 VS Code WebView 中播放。

因此，“VS Code 里的版本”与 4399 页面的完整 SWF 是同一个素材和代码来源。Ruffle
只是播放器，扩展本身不能提供更高清或不同版本的资源。

## 3. 主体结构

游戏根容器的导出类是 `EVERYTHING`（符号 1190）。关键子对象：

| 深度 | 符号 | 名称 | 作用 |
| ---: | ---: | --- | --- |
| 1 | 685 | `scene1` | 场景背景层，10 帧 |
| 5 | 708 | `scene2` | 场景前后景装饰层，10 帧 |
| 9 | 771 | `scene3` | 主要平台美术层，10 帧 |
| 18 | 1163 | `platform` | 平台碰撞、出生点和镜头边界，10 帧 |
| 77 | 1167 | `player1` | 完整玩家对象 |
| 106 | 1169 | `player2` | AI/第二玩家对象 |
| 165 | 1189 | `hud` | HUD 容器 |

符号 1163 中的蓝色小方块、红色线段、`lowest`、`farleft`、`farright` 和 `flag1`～`flag5`
是逻辑标记，并非正式美术。地图画面需要从 685、708、771 三层取图，碰撞和出生点则从
1163 读取；不能把 1163 的静态导出图直接当地图贴图。

## 4. 玩家组合结构

`PLAYER_FULL`（符号 1167）不是一张固定图片，而是运行时组合对象：

| 符号 | 实例名 | 作用 |
| ---: | --- | --- |
| 283 | `shade` | 脚底阴影 |
| 1164 | `frame` | 玩家主色底层 |
| 46 | `body` | 衣服/身体外观帧 |
| 95 | `head` | 头部、脸和帽子 |
| 444（初始） | `hand1` | 手臂与武器对象，运行时按武器替换 |
| 1166 | `bob` | 状态/动作附加层 |

玩家颜色、衣服、脸、帽子和武器不是靠整张贴图换色，而是分别切换这些子对象的时间轴帧。
当前验证版通过替换 `hand1` 对应符号并重新渲染 `PLAYER_FULL`，获得五把默认武器的正确
组合外观。动作时则使用不含 `hand1` 的玩家基底，叠加武器控制器导出的原始时间轴帧。
第二玩家暂时对主色层做了红色替换，后续应直接复刻原作的分层换色逻辑。

原作人物移动不是传统的左右走路帧。`PLAYER_FULL` 每帧根据水平速度、垂直速度和 `bob`
层的高度，对头、身体和持枪手进行不同速度的缓动，转向时再水平翻转。当前版本已按这个
原理接入轻微的移动起伏、空中偏移和持枪手滞后，而不是人为制作新的走路循环。

## 5. 武器映射

### 五把默认武器

| 游戏编号 | 名称 | 控制器符号 | 底层枪图符号 |
| ---: | --- | ---: | ---: |
| 1 | Sand Hawk | 510 `wpn_deagle` | 230 `gun_deagle` |
| 2 | Dual Cool Pistols | 513 `wpn_dual` | 243/253 等双枪组件 |
| 3 | Angry Cow | 526 `wpn_revolver` | 222 `gun_bull` |
| 4 | Bling Pistol | 507 `wpn_bling` | 214 `gun_49` |
| 5 | Katana | 522 `wpn_katana` | 刀身与手臂组合组件 |

已接入验证版的原始动作区间：

| 武器 | 主攻击帧 | 副攻击帧 |
| --- | --- | --- |
| Sand Hawk | 1–11 | 97–126 |
| Dual Cool Pistols | 1–5 | 60–63 |
| Angry Cow | 1–16 | 116–141 |
| Bling Pistol | 1–9 | 97–127 |
| Katana | 1–25 | 30–49 |

这些帧按原作 35 Hz 每个逻辑帧前进一格，并在玩家选定武器时预先装入，避免第一次开火时
现场读取贴图造成顿挫。

### 已定位的箱内武器控制器

`wpn_ak` 444、`wpn_bat` 450、`wpn_bomb` 452、`wpn_bow` 461、`wpn_homing` 462、
`wpn_knife` 466、`wpn_m4` 467、`wpn_mini` 470、`wpn_mp5k` 471、`wpn_shotgun` 472、
`wpn_sniper` 476、`wpn_umbrella` 483、`wpn_uzi` 484。

武器箱为 `CRATE`（符号 265），共有 8 帧。弹丸主体位于符号 487～501。

## 6. 血条结论

`player_healthdisplay` 是符号 403，HUD 面板逻辑位于 1188/1189。血条显示值每个 35 Hz
逻辑帧执行：

```text
显示血量 += (真实血量 - 显示血量) / 3
```

这解释了 4399 新版里血条变化自然但反应很快的观感。当前 Godot 版本已经使用同一公式。

## 7. 当前接入状态

本节描述“已经从原包确认并接入过的能力”，不代表当前构建的每项人工验收都通过。实时可运行状态、测试时间和阻塞问题以 [`STATUS.md`](STATUS.md) 为准。

已接入本地运行验证：

- `scene3` 全部十帧地图美术，均已按原舞台坐标裁切为 1000 × 560。
- `scene1` 全部十帧背景层和 `scene2` 全部十帧装饰层，与 `scene3` 按深度 1 / 5 / 9 的原始顺序合成。
- 三层对应的舞台裁切起点分别为 `scene1 +173,+57`、`scene2 +381,+157`、`scene3 +485,+160`，合成结果仍为 1000 × 560。
- 符号 1163 全部十帧中的黑色平台区域已转为可玩的单向顶面；第 2–10 张应用了导出碰撞层与 `scene3` 之间的 12 像素纵向校正。
- 五把默认武器对应的 `PLAYER_FULL` 静态组合图。
- 五把默认武器的主攻击、副攻击原始时间轴，共 179 帧。
- 基于原作速度缓动思路的移动起伏、空中偏移和持枪手滞后。
- 原版黄色问号武器箱。
- 按原版构图重绘的白色 HUD、绿色缓动血条、残机心形显示。
- 原版死亡爆炸组合：符号 102 扩张闪光、符号 110 的 15 帧冲击波、符号 105/107 两组粒子、符号 100 的三帧身体碎片，以及 `KABOOM!` 字样。
- 与死亡爆炸同步的 15 帧整屏震动；碎片继承角色死亡瞬间的横向速度。
- 符号 164 `fx_stun` 黄色问号：眩晕计时先减 1，每逢剩余帧数能被 7 整除时生成；目标缩放为 200%～300%，先快速放大再每帧缩小 10%。
- `resetplayer()` 的原始重生位置：`Y=-500`，`X=250+random(500)`；当前版本已经采用相同的屏幕外下落方式。
- 原版两条命中声音、四条掉落死亡声音和四条爆炸声音随机池；五把默认武器的 `smg2`、`pistol0`、`pistol3`、`whoosh` 调用均已绑定回对应动作帧。
- 原版默认武器换弹长度与声音节点：Sand Hawk / Bling Pistol 55 帧，Dual Cool Pistols 56 帧，Angry Cow 60 帧；`pistol_slide`、`pistol_mag`、`bolt1` 已绑定到其原始相对帧。
- `fx_jump2`（符号 352）使用符号 351 的十帧地图配色；高速落地时按原脚本生成 5 个，水平速度逐帧乘 0.9，缩放每帧随机衰减 3%～9%，同时随机播放 `drop1`～`drop3`。
- 武器箱拾取的 `fx_crateopen` / `fx_crateopen2`（符号 18 / 20）已按 4+2 数量和原始抛射参数接入；`fx_ex_wavesmall`（符号 111）的 15 帧画面及趋近 400% 的缩放公式也已接入。

`PLAYER_FULL` 中没有独立的普通受击角色时间轴；原作普通受击主要依靠击退、HUD 震动、短暂停顿和状态粒子表达。
当前尚未接入默认武器和箱内武器的完整原始换弹画面、枪口火焰与弹壳原始贴图，也未接入十张地图内部影片剪辑的动态装饰；13 把箱内武器的战斗逻辑已接入，视觉先使用本地验证用程序化效果。
本轮已补上与开火动作同步的程序化弹壳抛出、独立的短时弹道和换弹时中断上一段攻击时间轴；这些是接入原始贴图前的本地验证反馈，不作为公开分发美术资源。
同时已补上重生后 35 个逻辑帧的伤害保护、受击闪红反馈，以及换弹期间的攻击画面清理；当前研究版的核心对局反馈已闭环。

当前第二阶段先处理进入对局前的菜单界面。菜单使用已验证的原版黑底、白字和高对比色方向，加入地图预览、双玩家武器卡、准备状态和操作提示；Web 导出包已在本地浏览器中验证可加载并进入对局。

### 7.1 菜单框架逆向对应

原包体已经确认包含进入对局前菜单的文本和时间轴逻辑：

- `MENU_main`（符号 376）以及 `menu_editedit`（符号 1032）对应主菜单和 Custom Game 菜单容器。
- `DefineSprite_1033/frame_1/DoAction.as` 中的 `continue1()`、`continue2()`、`startgame()` 对应地图选择、玩家设置和开始对局的逐级跳转。
- 文本资源包含 `Map Selection`、`CLEAR SLOT`、`Primary Weapon`、`Perks`、`Team Deathmatch`、`Domination`、`CONTINUE` 和 `START!`。
- `PLAYER_FULL`（符号 1167）和 `PLAYER_AI`（符号 1169）用于玩家槽位中的角色预览和 AI 槽位标记。

Godot 当前已按截图建立对应的菜单骨架：主菜单 → Custom Game 模式选择 → Map Selection → 四槽位 Player Setup。玩家卡片的 `GUN` 和 `PERK` 按钮分别打开 Primary Weapon、Perks 弹窗；弹窗使用已经接入的玩家/武器逆向贴图，并保留帽子、衣服、脸部、颜色和未实现 Perks 效果的接口。原版主菜单中的双 AI 动态对战背景仍按此前计划使用静态占位图，不在本轮实现。

### 7.2 Perks 战斗逻辑

原版玩家脚本的 `perknumber` 使用 1～6 编号，当前 Godot 内部使用 0～5 对应同一顺序：

| 原版编号 | 技能 | 逆向证据 | 当前实现 |
| ---: | --- | --- | --- |
| 1 | No Perk | 默认值，无特殊分支 | 无额外效果 |
| 2 | Triple Jump | `PLAYER_FULL` 初始化时将 `maxjumps` 改为 3 | 每次出生/落地恢复三次跳跃 |
| 3 | No Recoil | 默认武器和箱内武器开火脚本跳过自身 `vx` 后坐 | 抑制玩家开火造成的自后坐；保留被敌方命中的击退 |
| 4 | Extra Ammo | `fillammo()` 将容量乘以 `1.33` 并取整 | 默认武器和箱内武器容量均增加 33% |
| 5 | Random Weapon at Spawn | `resetplayer()` 执行 `switchweapon(20 + random(13))` | 每条命从当前已接入的箱内武器池随机出生，耗尽后回到默认武器 |
| 6 | Infinite Primary Weapon Ammo | 各武器开火脚本跳过 `weaponammo -= 1` | 不消耗弹药且不进入换弹 |

原版 `BULLET` 脚本对受击者仍直接增加 `vx`，因此 No Recoil 的实现严格按逆向脚本解释为“取消自己的武器后坐”，不是免疫敌方攻击的击退。Perks 的 UI 描述已同步原版含义；Random Weapon 的随机池已经覆盖全部 13 把箱内武器。

### 7.3 当前武器主/副攻击审计

本轮对五把默认武器和 13 把箱内武器逐项检查了控制器脚本、伤害/击退路径和弹药消耗。默认武器保留原始玩家动作时间轴；箱内武器先按原始控制器参数接入可玩逻辑，视觉动作使用验证用占位表现。结果如下：

| 武器 | 普通攻击 | 特殊攻击 | 审计结果 |
| --- | --- | --- | --- |
| Sand Hawk | `BULLET`，伤害参数 23，弹药 -1 | 投掷枪，命中后击退并眩晕 | 已按原版主/副攻接入 |
| Dual Cool Pistols | 双枪轮流射击，伤害参数 9.5，弹药 -1 | 第二把枪同逻辑 | 已按原版节奏接入 |
| Angry Cow | 伤害参数 1337，折算为高速重弹 | 伤害参数 1338，速度更高 | 已按原版参数接入 |
| Bling Pistol | 伤害参数 19，弹药 -1 | 触发金钱效果并扣除自身生命 | 已按原版行为接入 |
| Katana | 两段近战判定 | 上挑、竖直击退和停顿 | 已按原版范围/时序接入 |
| Shotgun | 5 发散射，弹药 -1 | 8 发散射，弹药 -2，并显示 BOOM | 已按原版接入；视觉仍有占位效果 |
| M4 | 单发步枪弹，弹药 -1 | 5 发散射，弹药 -4 | 已按原版接入；视觉仍有占位效果 |
| Homing Missile | 追踪导弹，伤害 32、转向 1.1 | 玩笑导弹，转向 0.5 | 已按原版接入；视觉仍有占位效果 |
| AK-47 | 步枪弹，伤害参数 18.5 | 枪托近战，伤害 9、击退 22.5 | 已按原版接入 |
| Baseball Bat | 近战伤害 34、击退 70 | 命中失败投出棒球，伤害 10 | 已按原版接入 |
| Bow | 箭矢伤害 20、击退 34 | 三箭角度 -13° / 7° / -3°，弹药 -3 | 已按原版接入 |
| Sniper | 伤害参数 65、散布 1 | 无散布高伤害 BOOM 射击 | 已按原版接入 |
| MP5K | 伤害参数 14、高速连射 | 三发点射，弹药 -3 | 已按原版接入 |
| UZI | 伤害参数 13、高速连射 | 伤害参数 11.5、宽散布 | 已按原版接入 |
| Mini Gun | 伤害参数 13、高速连射 | 同款连续射击 | 已按原版接入 |
| Umbrella | 近战伤害 17、击退 28 | 按住开启，阻挡正面投射物和近战 | 已按原版接入 |
| Throwing Knife | 近战伤害 25 | 投掷飞刀，伤害 10、击退 19 | 已按原版接入 |
| Bomb | 弹跳炸弹，爆炸伤害 32 | 直线重炸弹，爆炸伤害 32 | 已按原版接入 |

原包体中的 13 把箱内控制器均已迁移为可玩的主/副攻击，并按各自原控制器长度驱动程序化动作。`DefineSprite_444_wpn_ak` 同时有 68 帧导出的持枪层，已接入 Godot：主攻击映射 1–32 帧，枪托特殊攻击映射 45–68 帧。其余 12 把在此前的选择性导出目录中没有生成完整逐帧持枪 PNG；重新扫描完整 SWF 后确认它们均有独立 MovieClip 和嵌套 Shape/影片剪辑依赖，目前仍使用逐武器形状、动作进度和反馈触发点的程序化兼容层，直到递归栅格化提取完成。

战斗反馈触发点也已按原脚本对齐：`fx_muzzleflash` 用于枪口火焰，`fx_shell` / `fx_shell2` 区分普通与大号弹壳，`fx_shrapnel` 用于散射/命中碎片，`fx_text` / `fx_textbig` 用于 `POW`、`BOOM!`、`KABOOM!`、`SNIPED`、`SHANKED` 等战斗文字。当前公开工程使用程序化绘制这些效果，原始效果 PNG 仍受本地研究资源边界约束。

菜单收尾还加入了 `Controls & Settings` 页面：默认英语，可用鼠标或 `Z` / 方向键切换到中文；地图、玩家设置、武器/Perks 弹窗和预留页面均保留 `X` / `Esc` 返回提示，鼠标靠近可交互区域会显示高亮描边。中文文本已经以有效 UTF-8 写入，但这不是原包素材结论；当前 Godot 默认后备字体不含 CJK 字形，中文模式仍需项目内字体资源后才能通过验收。

## 8. 分发边界

`reference/original/` 和 `MayhemArena/assets/original_reference/` 均已加入 `.gitignore`。
它们用于本机比对和逆向验证，不应直接提交、打包或公开发布。若项目需要公开发布，应使用
同样的粗线条 Flash 矢量卡通语言重绘角色、地图、武器、界面，并替换原作名称、音乐与音效。
