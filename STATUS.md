# Mayhem Arena 总进度面板

> 最后核对：2026-09-08（Asia/Singapore）
> 当前里程碑：第二阶段——对局前菜单稳定化与可用性修复
> 当前状态：🟡 源码可解析、自动测试通过；中文模式因缺少中文字形暂不可用
> 实时状态以本文件为准；`PROJECT.md` 记录长期范围，`REVERSE_ENGINEERING.md` 记录原版依据。

## 1. 一眼看懂当前进度

| 项目 | 当前结果 |
| --- | --- |
| 研究用可玩版本 | 约 75%；双人核心对局、十张静态地图、18 把武器逻辑、6 个 Perks 和菜单主流程已存在 |
| 可公开发布版本 | 约 25%；仍依赖本地原版参考素材，正式重绘、授权清理和跨平台发布尚未完成 |
| 当前源码 | Godot 4.7.2 可解析；2026-09-08 最近一次冒烟测试通过 |
| 当前主要故障 | 中文文案是有效 UTF-8，但项目没有 CJK 字体；`ThemeDB.fallback_font` 对已抽查中文字符全部返回无字形 |
| Web | 导出预设和 Web 包已生成；英文流程曾完成浏览器验证，中文显示验收未通过 |
| macOS / Windows 包 | 按当前计划暂缓；尚无可交付测试包 |
| Git | `main` 已建立；最近可运行功能提交为 `df6682c`，包含菜单、Perks、13 把箱内武器、测试和 Web 导出配置 |

上表百分比是工程估算，用于排序，不是自动计算值。若目标只是本机研究和手感复刻，以“研究用可玩版本”为准；若目标变为公开发布，必须重新评估素材、名称、声音和地图的重绘工作量。

## 2. 模块进度

| 模块 | 状态 | 已完成 | 仍需完成 |
| --- | --- | --- | --- |
| 工程基础 | ✅ | Godot 4.7.2、1000×560、固定 35 Hz/35 FPS、Compatibility 渲染 | 正式构建脚本和版本号体系 |
| 本地双人核心循环 | ✅ | 移动、二段跳、单向平台、生命、残机、击退、死亡、重生、胜负 | 继续做人工手感回归，只修阻断问题 |
| 五把默认武器 | ✅/🟡 | 主副攻击、原版参数、179 帧攻击动作、换弹时序和基础声音 | 完整原版换弹、枪口火焰和弹壳贴图 |
| 13 把箱内武器 | ✅/🟡 | 全部主副攻击逻辑、弹药消耗、随机武器池 | 原始逐帧持枪/攻击动画、弹丸和音效细化 |
| 六个 Perks | ✅ | Triple Jump、No Recoil、Extra Ammo、Random Weapon、Infinite Ammo 等均已接入 | 菜单说明和组合回归测试 |
| 地图 | ✅/🟡 | 十张 `scene1/2/3` 静态合成图、碰撞顶面和出生点 | 地图内部影片剪辑动态装饰 |
| 战斗反馈 | ✅/🟡 | 血条缓动、HUD 震动、死亡爆炸、眩晕、落地尘土、箱体碎片、受击闪红、重生保护 | 箱内武器原始特效、剩余透明通道问题 |
| 菜单 | 🟡 | Main → Custom Game → Map Selection → Player Setup → Gun/Perk 弹窗；鼠标和键盘导航 | 修复中文字体；人工遍历全部返回路径；AI 动态背景仍为静态占位 |
| 本地化 | 🔴 | 中文文案和切换入口已经写入 UTF-8 源码 | 打包可再分发 CJK 字体，并验证桌面端与 Web 全部页面 |
| Web 导出 | 🟡 | `export_presets.cfg` 和 Web 构建已生成，英文流程曾验证 | 中文字体修复后重新导出并完成多页面、多按键回归 |
| macOS / Windows | ⏸️ | 目标平台已确定 | 导出模板、测试包、真实 Windows 键盘/声音/全屏验证 |
| Git 管理 | ✅ | `main`、首次基线 `bd5fae6` 和功能检查点 `df6682c` 已存在；忽略规则保护本地 SWF/导出素材 | 中文修复完成后建立独立提交，并持续按里程碑提交 |
| 正式可分发美术 | ⏸️ | 视觉语言和需要替换的范围已有记录 | 重绘角色、地图、武器、UI，并替换原作名称、音乐和音效 |

状态含义：✅ 已达到当前研究版目标；🟡 已可用但仍有明确缺口；🔴 当前阻塞；⏸️ 已知且按计划暂缓。

## 3. 最近一次验证基线

2026-09-08 对当前工作区执行：

```text
Godot 主场景解析：通过
smoke_test.gd：通过
35 Hz / 十张地图 / 五把默认武器 / 六个 Perks：通过
13 把箱内武器配置和基础行为：通过
攻击、换弹、声音、弹壳、死亡、眩晕、落地、武器箱反馈：通过
中文字体字形探针：失败；抽查中文字符全部 has_glyph=false
```

沙盒运行时出现的 `user://logs` 写入和 macOS CA 证书提示不属于游戏脚本失败；判断测试结果时仍需检查退出码和是否出现 `SCRIPT ERROR`。

当前尚缺一轮中文修复后的人工验收。自动测试通过不等于菜单每个页面的排版、鼠标热区和中文显示已经通过。

## 4. 当前问题与优先级

| 编号 | 优先级 | 问题 | 状态/判定 |
| --- | --- | --- | --- |
| BUG-003 | P0 | 切换中文后文字成为缺字占位/乱码 | 已定位：所有菜单直接使用 `ThemeDB.fallback_font`，项目没有字体文件，默认字体不含 CJK；不是 UTF-8 编码损坏 |
| BUG-001 | P1 | 部分人物贴图可能残留矩形或色块 | 已加入运行时透明清理与最近邻过滤，仍需 Web 人工复核 |
| BUG-002 | P1 | 箱内武器和部分战斗动画仍是程序化占位 | 逻辑已经接入，原始逐帧贴图与透明通道仍待补齐 |
| QA-001 | P1 | 当前菜单大改后缺少完整人工路径回归记录 | 中文修复后一次性覆盖主菜单、地图、四槽位、Gun/Perk 弹窗、返回和开局 |
| EXPORT-001 | P2 | macOS / Windows 测试包未生成 | 当前暂缓，不阻塞中文和菜单收尾 |

## 5. 下一对话应按此顺序推进

### P0-1：修复中文字体

1. 先读本文件、`PROJECT.md`、`REVERSE_ENGINEERING.md`，再查看 `git status` 和当前差异；不要重置或覆盖未提交功能。
2. 选择允许随项目再分发、同时覆盖简体中文和常用符号的字体，例如采用 OFL 许可的 Noto Sans CJK SC，并把字体及许可文件放入可跟踪目录 `MayhemArena/assets/fonts/`。
3. 不要依赖 macOS 系统字体。Web 和 Windows 必须使用项目内字体资源。
4. 为菜单建立统一字体入口。至少替换 `game.gd` 中的 `ThemeDB.fallback_font`；同时检查 `player_hud.gd` 和 Perk 图标文字是否需要同一字体或字体 fallback。
5. 保留原版英文菜单的粗体、高对比感觉；中文可使用独立 CJK 字体，不要求用中文字体替代所有英文标题。
6. 增加自动断言：所选字体对“中文混乱大枪战控制设置返回”均具有字形。

### P0-2：完整验证

依次执行：

```bash
tools/godot/Godot.app/Contents/MacOS/Godot --headless --path MayhemArena --quit-after 5
tools/godot/Godot.app/Contents/MacOS/Godot --headless --path MayhemArena --script res://tests/smoke_test.gd
tools/godot/Godot.app/Contents/MacOS/Godot --path MayhemArena
```

人工路径：

```text
Main Menu
→ Controls & Settings
→ 切换中文
→ 返回主菜单
→ 自定义游戏
→ 自由混战
→ 十张地图选择
→ 四个玩家槽位
→ Gun / Perk 弹窗
→ 两名玩家准备
→ 进入对局
→ Esc 返回
```

随后重新导出 Web 包，通过本地 HTTP 服务验证中文，不要直接双击 `index.html`。至少检查主菜单、设置、地图选择、玩家设置、弹窗和对局 HUD。

### P0-3：建立中文修复检查点

1. 执行 `git diff --check`、`git status --short`，确认没有缓存、构建包、原始 SWF 或 `assets/original_reference/` 被暂存。
2. 菜单、Perks 和 13 把箱内武器已保存在功能检查点 `df6682c`；本轮只需审查中文字体、测试和文档差异。
3. 测试全部通过后建立独立中文修复提交；提交信息应描述实际里程碑，不要使用“临时修复”。
4. 提交后把新提交号、测试时间和剩余问题写回本文件。

## 6. 下一里程碑验收条件

- 当前源码和冒烟测试均通过，无 `SCRIPT ERROR`。
- 中文切换后不存在缺字方框、乱码或回退到不可控系统字体。
- 桌面端和 Web 的主菜单、设置、地图选择、玩家设置、Gun/Perk 弹窗均能显示完整中文。
- 英文模式没有因字体修复而发生明显排版倒退。
- 两名玩家仍能从菜单进入并完成一局对战。
- Git 工作区形成一个可复现、可回退的新检查点。
- `STATUS.md`、`PROJECT.md` 的阶段说明和已知 Bug 同步更新。

## 7. 不得破坏的项目约束

- 玩法和显示保持固定 35 Hz/35 FPS，不改成 60 Hz，不启用物理插值。
- 保留原作允许的粗糙街机物理，不为偶发穿模引入复杂连续碰撞。
- 用户已经否决原创暖色工业荒漠美术；视觉方向继续参考 Redux 的粗线条 Flash 矢量卡通风格。
- `reference/original/` 和 `MayhemArena/assets/original_reference/` 只用于本地研究，继续保持 Git 忽略，不能作为公开发布资源。
- 不要删除或重置当前未提交改动；先阅读差异，再按功能拆分和提交。
- 完成一个里程碑后更新本文件，不要只在对话中口头报告。

## 8. 文档职责

| 文件 | 用途 | 更新时机 |
| --- | --- | --- |
| `STATUS.md` | 实时总进度、当前故障、测试结果、下一对话交接 | 每个工作阶段结束时 |
| `PROJECT.md` | 产品目标、技术选择、长期范围、里程碑和正式 Bug 条目 | 范围或里程碑变化时 |
| `REVERSE_ENGINEERING.md` | SWF 来源、符号、帧号、参数和接入证据 | 新逆向结论或映射变化时 |
