# Mayhem Arena 总进度面板

> 最后核对：2026-09-09（Asia/Singapore）
> 当前里程碑：P2 玩法功能 Bug 全面收口
> 当前状态：🟡 等待用户继续实机清点；当前只修玩法、武器、地图可达性与 AI Bug，全部关闭后再进入 P3 全项目矢量重绘
> 实时状态以本文件为准；`PROJECT.md` 记录长期范围，`REVERSE_ENGINEERING.md` 记录原版依据。

## 1. 一眼看懂当前进度

| 项目 | 当前结果 |
| --- | --- |
| 研究用可玩版本 | 约 89%；四槽自由配置、AI 平台追击/边缘刹车/击飞救场、18 把武器逻辑、13 套箱内武器父级动画、线状弹丸、延迟箱体警告、6 个 Perks 和菜单主流程已存在 |
| 可公开发布版本 | 约 25%；仍依赖本地原版参考素材，正式重绘、授权清理和跨平台发布尚未完成 |
| 当前源码 | Godot 4.7.2 可解析；2026-09-09 主场景和冒烟测试通过，含四槽任意单人开局、AI-3 救场、特殊武器透明清理、线状弹丸和箱体 250 帧警告断言 |
| 当前主要剩余项 | BUG-010～021、用户后续实机发现的玩法问题，以及 AI-4 至 AI-7；本阶段不启动大规模美术替换 |
| Web | 上一轮菜单、中文和对局路径已经通过；本轮透明枪口/弹壳与地图帧修正尚未重新导出，按用户要求在本轮全部工作完成后统一更新 |
| macOS / Windows 包 | 按当前计划暂缓；尚无可交付测试包 |
| Git | `main` 按里程碑提交；最新功能检查点为 `f68dba5`（AI 平台移动与救场），战斗视觉与四槽基础保存在 `c1fcae3` |

上表百分比是工程估算，用于排序，不是自动计算值。若目标只是本机研究和手感复刻，以“研究用可玩版本”为准；若目标变为公开发布，必须重新评估素材、名称、声音和地图的重绘工作量。

## 2. 模块进度

| 模块 | 状态 | 已完成 | 仍需完成 |
| --- | --- | --- | --- |
| 工程基础 | ✅ | Godot 4.7.2、1000×560、固定 35 Hz/35 FPS、Compatibility 渲染 | 正式构建脚本和版本号体系 |
| 本地双人核心循环 | ✅ | 移动、二段跳、单向平台、生命、残机、击退、死亡、重生、胜负 | 继续做人工手感回归，只修阻断问题 |
| 完整强度战斗 AI | 🟡 | 四槽 HUMAN/AI/空置状态、任意单槽开局、统一控制层、最近敌人追踪、十图平台图、边缘刹车、跨平台跳跃和击飞救场 | AI-4 起完成 18 把武器的射程、主副攻击、弹药和换弹决策，再接战术层 |
| 五把默认武器 | ✅/🟡 | 主副攻击、原版参数、179 帧攻击动作、换弹时序和基础声音；枪口/弹壳已接入递归导出资源 | 逐把桌面/Web 视觉验收；命中/爆炸原图仍可继续替换 |
| 13 把箱内武器 | ✅/🟡 | 全部主副攻击逻辑、弹药消耗、随机武器池；按原控制器长度播放动作；已从 `PLAYER_FULL` 父级递归合成并接入 13 套透明持枪帧，共 834 帧；原版枪口/两类弹壳符号已接入并保留缺资源回退 | 逐把桌面/Web 视觉验收；箱内武器按原规则耗尽后回到默认武器，不新增换弹 |
| 六个 Perks | ✅ | Triple Jump、No Recoil、Extra Ammo、Random Weapon、Infinite Ammo 等均已接入 | 菜单说明和组合回归测试 |
| 地图 | ✅ | 十张静态地图、碰撞顶面和出生点；三层按 `selected_map` 固定取对应帧；递归检查确认三层选中帧没有额外多帧嵌套 MovieClip，因此不再错误循环混帧 | 仅保留逐图视觉验收；若后续发现独立装饰素材，再按单图回退接入 |
| 战斗反馈 | ✅/🟡 | 血条缓动、暖色命中反馈、原脚本式线状子弹、特殊弹丸辨识、延迟箱体警告、HUD 震动、死亡爆炸、眩晕、落地尘土、受击闪红和重生保护 | 原始 Shape 的逐像素替换和原版换弹人物画面仍可继续精修 |
| 菜单 | 🟡 | Main → Custom Game → Map Selection → 四槽 Player Setup；四格都可清空并重建 HUMAN/AI，任意一个非空即可开始，全空禁用 | AI 动态背景仍为静态占位；完整 AI 尚在 AI-4 至 AI-7 |
| 本地化 | ✅/🟡 | 项目内置 Source Han Sans CN Heavy；统一字体入口；中文切换和字形断言通过 | 继续观察不同浏览器下的排版细节 |
| Web 导出 | ✅/🟡 | 上一构建的浏览器入口、主菜单、地图选择和中文路径无日志错误 | 最新四槽、视觉修复与 AI 尚未重导出；待本轮大步骤收尾后统一更新 |
| macOS / Windows | ⏸️ | 目标平台已确定 | 导出模板、测试包、真实 Windows 键盘/声音/全屏验证 |
| Git 管理 | ✅ | `main`、首次基线 `bd5fae6` 和功能检查点已存在；忽略规则保护本地 SWF/导出素材；本轮 P1 收口内容已纳入当前提交 | 持续按里程碑提交 |
| 正式可分发美术 | ⏸️ | 视觉语言和需要替换的范围已有记录 | 重绘角色、地图、武器、UI，并替换原作名称、音乐和音效 |

### 里程碑顺序（已锁定）

| 顺序 | 大步骤 | 开始条件 | 完成条件 |
| ---: | --- | --- | --- |
| 当前 P2 | 玩法功能 Bug 全面收口 | 已开始；继续接收用户实机清点结果 | 所有已登记玩法 Bug 关闭，18 把武器最终规则完成，AI-4～AI-7 完成，35 Hz 自动测试与人工核心路径通过 |
| 下一 P3 | 全项目矢量重绘 | 用户完成本轮 Bug 清点并明确通知开始；P2 功能基线冻结 | 人物、武器、弹丸、战斗特效、地图动态层和 UI 逐步替换为自主矢量绘制；保持原作构图、粗黑轮廓、纯色填充、注册点和 35 帧动作节奏 |

P3 开始前不得为了重绘推翻当前可玩的贴图版本。现有原版参考贴图保留为本地对照和回退基线；重绘应分模块切换、截图叠加比较并逐项验收，不能一次性替换后再集中排错。

状态含义：✅ 已达到当前研究版目标；🟡 已可用但仍有明确缺口；🔴 当前阻塞；⏸️ 已知且按计划暂缓。

## 3. 最近一次验证基线

2026-09-09 对当前工作区执行源码与自动化验收：

```text
Godot 主场景解析：通过
smoke_test.gd：通过（含项目内 CJK 字形断言）
35 Hz / 十张地图 / 五把默认武器 / 六个 Perks：通过
13 把箱内武器配置、基础行为、834 帧父级合成资源加载、人物色块清理和第二玩家换色：通过
四槽自由清空/添加、任意单角色开局、统一 AI 控制和十图平台图：通过
线状子弹的显隐/缩放、特殊弹丸显示、250 帧后箱体警告、暖色命中反馈：通过
中文字体字形探针：通过；项目内 Source Han Sans CN Heavy 覆盖必需中文字符
Web 历史基线：中文设置、主菜单、模式/地图、玩家设置、Gun/Perk 弹窗、人物透明预览、进入对局地图/HUD、Esc 返回均通过
Web 当前结果：上一构建的入口、中文菜单和地图选择路径正常；最新四槽、战斗视觉与 AI 改动尚未重新导出
```

沙盒运行时出现的 `user://logs` 写入和 macOS CA 证书提示不属于游戏脚本失败；判断测试结果时仍需检查退出码和是否出现 `SCRIPT ERROR`。

当前源码与自动测试已经通过。新武器动画的桌面/Web 人工验收继续暂缓；AI-1 至 AI-3 已完成，下一步进入 AI-4 完整武器战斗。

## 4. 当前问题与优先级

| 编号 | 优先级 | 问题 | 状态/判定 |
| --- | --- | --- | --- |
| BUG-003 | ~~P0~~ ✅ | 切换中文后文字成为缺字占位/乱码 | 已修复：内置可再分发 CJK 字体，`game.gd`、HUD 和 Perk 图标统一使用项目字体；自动字形断言和 Web 中文页面检查通过 |
| BUG-001 | ~~P1~~ ✅ | 部分人物贴图可能残留矩形或色块 | 已加入运行时透明清理、最近邻过滤和玩家主色大连通背板清除；自动像素断言和本轮 Web 人工预览均通过 |
| BUG-002 | 精修待办 | 箱内武器和部分战斗动画仍需人工验收或原图替换 | 功能性缺口均已关闭；原始 Shape 逐像素替换、换弹人物画面和逐把桌面/Web 人工对照留作非阻塞美术精修 |
| BUG-004 | ~~P1~~ ✅ | 打击特效颜色与原版不一致，视觉不够醒目 | 已修复：普通命中改用红橙暖色，不再继承玩家主题色；伤害、击退和生命周期未变 |
| BUG-005 | ~~P1~~ ✅ | 特殊武器的静态贴图仍显示错误 | 已修复：静止持枪和 HUD 头像统一使用对应特殊武器父级合成首帧，不再回退到错误的通用枪图 |
| BUG-006 | ~~P1~~ ✅ | 武器箱从掉落开始就持续闪烁 | 已修复：按原箱体脚本在 250 帧前稳定显示，最后 8 帧播放临期警告后消失 |
| BUG-007 | ~~P1~~ ✅ | 播放特殊武器动画时角色位置出现色块 | 已修复：合成帧加载时清除低透明边缘和人物区域的导出蓝色背板；加入像素断言防回归 |
| BUG-008 | ~~P1~~ ✅ | 多种特殊武器的弹丸及飞行效果不正确 | 已修复功能表现：箭、追踪弹、玩笑弹、飞刀、棒球、炸弹、投枪和火箭均有独立朝向/轮廓/运动显示；精确原始 Shape 替换另列为美术精修 |
| BUG-009 | ~~P1~~ ✅ | 普通子弹缺少原版线状显示效果 | 已修复：依据 487/488 脚本保留第 2 帧显现、弱弹 70、高火力趋近 500、散弹趋近 350 且每帧缩短 16 的线状显示 |
| BUG-010 | P2 | AI 跳跃过于活跃，会出现持续跳跃 | 待修复：提高普通追击跳跃门槛并加入落地/路线状态约束；只在跨平台、追击高处目标、越障或击飞救场时主动跳跃，不能只因跳跃冷却结束就再次起跳 |
| BUG-011 | P2 | AI 获得 Triple Jump 后不会有计划地使用第二、第三段跳跃 | 待修复：AI 读取 `max_jump_count()` 与 `jumps_remaining`，按预计落点、竖直速度和离场风险决定空中续跳；普通路线不得为了耗尽跳数而连跳，第三段优先保留给高差路线或救场 |
| BUG-012 | P1 | 雨伞特殊攻击不能按原目标持续格挡 | 待修复：特殊攻击支持长按；按住期间持续撑伞并阻挡正面攻击，松开后收伞；不能按半自动单次触发处理 |
| BUG-013 | P1 | 炸弹碰撞、弹跳和延迟爆炸逻辑不正确 | 待修复：两种炸弹接触人物立即爆炸、接触平台不直接爆炸；普通炸弹落地弹跳且逐次减速，停下约 4 帧（约 0.1 秒）后爆炸；特殊炸弹不弹跳，落地后进入同一停下延迟爆炸流程 |
| BUG-014 | P1 | 小平台高度超过一段跳可达范围 | 待修复：逐图降低小平台或相邻小平台的高度差，使标准角色不用二段跳也能登上；不修改全局跳跃力度，不破坏平台粗糙物理 |
| BUG-015 | P2 | 线状子弹缺少发光层 | 待修复：在现有黑色轮廓和暖色内芯外增加轻量光晕，保留原线长、散弹缩短和 35 Hz 时序，避免光晕盖住人物或地图 |
| BUG-016 | P1 | Sniper 瞄准与隐身特殊攻击缺失 | 待修复：普通攻击按住时白色方向线渐显，约 11 帧（0.3 秒）完成瞄准；瞄准完成后锁定水平移动，但允许跳跃及左右转向，松开射击；短按也必须播满前摇。特殊攻击进入人物和枪支完全隐藏状态，被命中或进行攻击后显形 |
| BUG-017 | P1 | Angry Cow 特殊攻击缺少合线瞄准前摇 | 待修复：以枪口为顶点显示中心线上下各 30° 的两条白线，并在约 18 帧（0.5 秒）内合拢后完成瞄准；短按播满前摇后射击，长按完成后可带着瞄准线移动，松开射击 |
| BUG-018 | P2 | 弓箭飞行完全笔直 | 待修复：箭矢增加轻微重力，使单箭和三箭在飞行中小幅下落；保持当前初速度、角度、伤害和碰撞逻辑 |
| BUG-019 | P1 | Homing Missile 特殊攻击类型错误 | 待修复：移除特殊攻击的追踪能力；弹丸先直线飞行一段距离，再分裂为三发，分别沿原方向、向上 45°、向下 45°继续飞行 |
| BUG-020 | P1 | Mini Gun 普通攻击缺少启动前摇 | 待修复：按下普通攻击后先播放 7 个逻辑帧（35 Hz 下 0.2 秒）的启动前摇，再开始连续射击；前摇不得提前消耗弹药或生成子弹，松开后的取消/保持规则在实现时与武器动画一起验收 |
| BUG-021 | P1 | Throwing Knife 普通与特殊攻击功能颠倒 | 待修复：普通攻击改为投掷飞刀，特殊攻击改为近距离刺击；同步交换输入逻辑、菜单说明、动画区间、AI 距离判断和弹药消耗语义 |
| QA-001 | ~~P1~~ ✅ | 当前菜单大改后缺少完整人工路径回归记录 | 自动覆盖主菜单→模式→地图→玩家设置→Gun/Perk→返回；本轮 Web 已人工覆盖中文设置、两级菜单、弹窗、人物预览、对局地图/HUD 和 Esc 返回 |
| AI-001 | P2 | AI 已能选敌、沿平台追击、边缘刹车、跨平台跳跃、击飞救场和基础开火，但还不是完整强度 | AI-1 至 AI-3 已完成；当前进入 AI-4，补齐 18 把武器的主副攻击和弹药决策 |
| EXPORT-001 | P2 | macOS / Windows 测试包未生成 | 当前暂缓，不阻塞中文和菜单收尾 |

## 5. 下一对话应按此顺序推进

### 已完成：P0 中文字体与 Web 收尾

1. 已将 Source Han Sans CN Heavy 及许可说明放入 `MayhemArena/assets/fonts/`。
2. 已建立 `FontCatalog` 统一字体入口，替换菜单、HUD 和 Perk 图标的系统回退字体。
3. 已增加必需中文字符自动断言，并在 Web 包中验证中文页面。

### 已完成：P1 菜单与战斗反馈验收基线

本轮源码项已集中完成，并按一次性收尾原则执行了最后一轮：

```bash
tools/godot/Godot.app/Contents/MacOS/Godot --headless --path MayhemArena --quit-after 5
tools/godot/Godot.app/Contents/MacOS/Godot --headless --path MayhemArena --script res://tests/smoke_test.gd
tools/godot/Godot.app/Contents/MacOS/Godot --path MayhemArena
```

人工路径：

```text
Main Menu
→ Controls & Settings
→ 切换中文
→ 返回主菜单
→ 自定义游戏
→ 自由混战
→ 十张地图选择
→ 四个玩家槽位
→ Gun / Perk 弹窗
→ 两名玩家准备
→ 进入对局
→ Esc 返回
```
Web 包已在字体接入后重新导出，并通过本地 HTTP 服务检查中文页面；后续只需针对 Gun/Perk 弹窗、对局 HUD、透明色块和原始视觉缺口做人工复核。

本轮新增修复：玩家设置页底部提示的 `X BACK` 现在会返回地图选择，不再误当作取消准备；Godot 主场景解析和 `smoke_test.gd` 均已复核通过。

本轮新增修复：原版动态地图层只在对应的第一张地图中合成，其余九张地图继续使用各自静态美术，避免场景装饰与碰撞布局错配。

本轮新增回归：武器/技能弹窗现在支持方向键与左右键横向浏览；自动测试已覆盖主菜单、模式选择、地图选择、玩家设置、Gun/Perk 弹窗、弹窗返回和空白处安全返回。

本轮新增战斗视觉：接入逆向资源中的 AK-47 68 帧共享武器层，主攻击使用 1–32 帧，枪托特殊攻击使用 45–68 帧；其余箱内武器程序化表现的动作进度改为按各自完整时间线计算，不再固定在前 12 帧。

本轮新增战斗反馈：按逆向脚本中的 `fx_muzzleflash`、`fx_shell`、`fx_shell2`、`fx_shrapnel` 和 `fx_text` 触发点补齐逐武器枪口火焰、两类弹壳、散射碎片、命中/爆炸文字；`POW`、`BOOM!`、`KABOOM!`、`SNIPED`、`SHANKED` 等标签已接入对应主/副攻击和命中路径，并加入自动生命周期断言。

本轮最终验收：主场景解析通过；`smoke_test.gd` 通过；Web 导出成功；浏览器人工通过中文设置、主菜单、模式选择、地图选择、玩家设置、Gun/Perk 弹窗、人物透明预览、进入对局地图/HUD 和 Esc 返回。

最终 Web 人工检查发现第一张地图进入对局后仍出现白底，确认是透明影片层在 Web 渲染器中的兼容问题；现改为 Web 端使用原版 1000×560 静态底图，桌面端保留动态层。修复后统一导出并人工确认地图、HUD、玩家和 Esc 返回正常；后续可替换为预合成的 Web-safe 动态资源。

12 把箱内武器嵌套视觉依赖的递归栅格化已经完成；后续若继续追求原版逐像素一致，需要替换仍为兼容层的枪口、弹壳、换弹和部分命中/爆炸效果，并为 Web 预合成动态地图层。

### 视觉专项执行指导与验收标准（已执行，保留为回归清单）

这两个专项可以在 AI 主线之前或之后独立执行；开始时不要删除现有程序化效果，而是先把原始贴图作为可切换的新路径接入，全部验收通过后再移除兼容绘制。

#### VFX-1 原始枪口、弹壳与换弹贴图

| 顺序 | 工作 | 具体要求 | 完成判定 |
| --- | --- | --- | --- |
| 1. 建立原版映射表 | 从完整 SWF 核对 `fx_muzzleflash`（符号 151）、`fx_shell`（156）、`fx_shell2`（159）以及五把默认武器控制器 | 记录总帧数、舞台原点、缩放、旋转、颜色、生成帧和适用武器；开火触发点继续沿用现有逆向脚本证据，不凭观感重猜 | 每种效果都有“符号 → 帧 → 武器 → 触发时刻”的表格 |
| 2. 递归透明导出 | 优先用 FFDec/JPEXS 递归导出 MovieClip/Shape 依赖；SWFTools 只用于确认符号和脚本 | PNG 保留透明通道、原始朝向与注册点；若独立导出缺层，采用与 13 把箱内武器相同的父级合成法，禁止用白底截图代替 | 所有导出帧无白底、无矩形色块、无缺失子元件 |
| 3. 接入枪口火焰 | 用原图序列替换 `player.gd` 的 `draw_muzzle_flash()` 显示层 | 仍由当前武器开火事件触发，锚定 `muzzle_position()`；朝左时围绕注册点镜像，不改变弹丸出生位置、伤害、后坐力和射速 | 18 把武器开火时位置、朝向和帧速正确，35 Hz 下不多播或漏播 |
| 4. 接入两类弹壳 | 仅替换 `game.gd` 中 shell 的绘制外观 | 保留现有抛出速度、重力、旋转、寿命和 `fx_shell` / `fx_shell2` 分类；先核对散弹枪、M4 类和双持武器是否一次生成多个或使用不同偏移 | 普通/大号弹壳外观正确，左右朝向与抛出方向正确，现有 shell 自动测试继续通过 |
| 5. 接入换弹画面 | 以原武器控制器的 reload 区间驱动人物+武器合成帧 | 默认枪当前已知区间：Sand Hawk/Bling 58–93、Dual 79–120、Revolver 63–105；时长仍按已确认的 55/56/60 帧和原声音节点运行。箱内武器耗尽后仍按原规则丢弃，不擅自添加换弹 | 换弹开始、中断、结束、补弹时刻和声音均不回退；近战武器不出现枪械换弹 |
| 6. 保留与收口 | 原图路径先与程序化兼容层并存，缺帧时自动回退 | 增加资源存在、透明边界、生命周期、左右镜像、换弹中断和 35 Hz 帧序测试；桌面与 Web 各验收一轮后才删除对应兼容绘制 | 自动测试通过，五把默认武器逐把人工通过；13 把箱内武器的枪口/弹壳按实际适用范围通过 |

资源建议放在 `MayhemArena/assets/original_reference/effects/muzzle/`、`effects/shell/normal/`、`effects/shell/large/`；换弹人物合成帧继续放在 `player_layers/` 下并沿用脚底锚点。所有原版资源继续受 Git 忽略，仅限本机研究版。

#### MAP-VFX-1 十张地图内部动态装饰

先纠正当前认识：原脚本明确执行 `scene1.gotoAndStop(mapnumber)`、`scene2.gotoAndStop(mapnumber)`、`scene3.gotoAndStop(mapnumber)`，因此三组符号各自的第 1–10 帧代表地图 1–10。现有 `map_animation_frame` 循环 0–9 的做法不能作为真正动态装饰的完成结果，它有跨地图混帧风险。真正会动的内容位于每个地图选择帧内部的嵌套 MovieClip。

| 顺序 | 工作 | 具体要求 | 完成判定 |
| --- | --- | --- | --- |
| 1. 修正静态基线 | 对局中三层都固定到 `selected_map` 对应帧，不再把地图编号当动画帧循环 | 保留十张已验证静态底图、现有平台碰撞和出生点；先以截图对比确认每张地图没有串层 | 连续运行 30 秒，平台和背景不会切成其他地图 |
| 2. 建立嵌套清单 | 分别检查十张地图在 `scene1/scene2/scene3` 选中帧内的子 MovieClip | 记录子符号编号、层级、深度、注册点、循环长度、是否受摄像机视差影响；静态 Shape 不重复做成动画 | 十张地图各有一份“背景/中景/前景动态元件”清单 |
| 3. 逐个递归导出 | 用 FFDec 递归渲染每个动态子剪辑的完整循环 | 导出透明 PNG 序列并保留注册点；不同长度使用各自循环周期，不强行统一成 10 帧；必要时在完整父级中隔离目标子剪辑渲染 | 每个序列透明正确，首尾循环无跳变，无缺失嵌套依赖 |
| 4. Godot 分层接入 | 每张地图建立独立装饰配置，按原深度放在玩家后方或前方 | 动画按固定 35 Hz 前进；装饰不得参与碰撞、伤害或 AI 平台图；保留原脚本的视差关系，镜像地图时同步镜像注册点与移动方向 | 十张地图只播放自己的装饰，遮挡顺序和镜像正确，不影响对局逻辑 |
| 5. Web-safe 输出 | 不直接依赖当前会出现白底的透明大画布层 | 优先导出紧边界小图；确需整层合成时预乘/清理透明色并预合成 Web-safe 帧。Web 失败时继续回退静态底图，桌面路径不受影响 | 桌面和 Web 均无白底、色块、明显闪烁，内存和加载时间可接受 |
| 6. 分图验收 | 按地图 1→10 逐张完成，不一次性替换全部 | 每完成一张就核对静态底图、碰撞顶面、角色遮挡、35 Hz 动画、镜像和 Web 回退，并建立测试/提交检查点 | 十张地图逐项勾选完成；任一地图失败只回退该地图，不拖累其他地图 |

推荐实施顺序是：先修正“地图编号被当作动画帧”的基线，再完成地图 1 的真实嵌套动画作为模板，确认桌面与 Web 导出方案后复制到地图 2–10。主菜单 AI 对战背景继续保持静态图片，不纳入本专项。

### 重新解包初检（2026-09-08）

使用完整文件 `reference/original/4399_gun_mayhem_redux_full.swf` 重新扫描，并用 SWFTools 对武器符号抽取。结果确认：`wpn_bat` 450、`wpn_bomb` 452、`wpn_bow` 461、`wpn_homing` 462、`wpn_knife` 466、`wpn_m4` 467、`wpn_mini` 470、`wpn_mp5k` 471、`wpn_shotgun` 472、`wpn_sniper` 476、`wpn_umbrella` 483、`wpn_uzi` 484 均存在独立 MovieClip，帧数分别为 76、48、51、61、42、79、75、65、78、94、46、51。

此前“缺少原始逐帧 PNG”的结论只适用于当前选择性导出目录，不适用于完整 SWF 本体。初步抽取已生成这些符号的独立 SWF；由于仍依赖嵌套 Shape、字体和影片剪辑，已改用 `PLAYER_FULL`（符号 1167）作为父级进行递归合成和逐帧栅格化。

### 父级时间轴逐帧提取（2026-09-09）

已完成第二条提取路径：保留 SWFTools 的独立控制器结果，同时用 FFDec 将每把武器注入 `PLAYER_FULL` 的 `hand1`，冻结人物基底的身体/脸/眼睛/帽子子时间轴，再按原控制器长度导出透明 PNG。13 套本地合成帧已按人物身体脚底统一到 220×180 的 2x 画布，共 834 帧，并接入 `player.gd` 的主/副攻击动作区间。

本轮确认的控制器与导出数量为：AK 444/68、Bat 450/76、Bomb 452/48、Bow 461/51、Homing 462/61、Knife 466/42、M4 467/79、Mini 470/75、MP5K 471/65、Shotgun 472/78、Sniper 476/94、Umbrella 483/46、UZI 484/51。狙击枪原始导出画布存在远端异常边界，已在人物锚点附近裁切，瞄准线仍保留在有效透明帧内。

这些资源位于 `MayhemArena/assets/original_reference/player_layers/composite/`，与 `reference/original/` 一样受忽略规则保护，仅用于本机逆向验证，不进入公开分发包。直接控制器导出保存在临时研究目录，用于符号编号和帧数交叉核对；本轮 Web 包已在源码和自动测试完成后统一更新。

### VFX-1 / MAP-VFX-1 本轮执行记录（2026-09-09）

本轮已按完整 SWF 的父级时间轴重新核对并接入：

| 项目 | 原版符号链 | 本地接入 | 关键验收点 |
| --- | --- | --- | --- |
| 枪口火焰 | `fx_muzzleflash` 151 → 形状 145 + 内层 MovieClip 150（4 帧形状 146–149） | `effects/muzzle/1.png`，FFDec 递归父级导出 | 以导出画布约 `(142,181.5)` 为注册点；按 `muzzle_position()` 绘制，左右镜像不改变弹丸出生点 |
| 普通弹壳 | `fx_shell` 156 → 155 → 154 | `effects/shell/1.png` | 保留抛出速度、重力、旋转和 24 帧寿命；缺资源回退程序化小矩形 |
| 第二类弹壳 | `fx_shell2` 159 → 158 → 157 | `effects/shell2/1.png` | M4/散弹枪类保留独立变体；缺资源回退原程序化大弹壳 |
| 换弹 | 默认控制器 reload 区间 | 现有人物 raw 帧与声音节点 | Sand Hawk/Bling 55 帧、Dual 56 帧、Revolver 60 帧；所有帧按固定 35 Hz 前进；箱内武器耗尽回到默认武器，不新增换弹 |

脚本触发核对结果：默认武器的枪口/弹壳仍由原控制器开火帧驱动；箱内武器的父级动作帧仍按各自完整控制器长度播放。原脚本中 `fx_shell2` 的额外触发位于 M4 第 63 帧和 Shotgun 第 49 帧（散弹枪两次）；当前可玩逻辑保留统一的弹壳生命周期与兼容回退，后续若追求逐事件像素级一致，再把这两个动作中的多发偏移独立拆出。

地图核对结果：`scene1/2/3` 的第 1–10 帧分别对应地图 1–10；FFDec XML 递归扫描未发现选中地图帧内部还有多帧嵌套 MovieClip，因此没有可继续单独导出的动态装饰序列。本地运行已改为三层固定取 `selected_map`，不再以 35 Hz 把 10 张地图混成一段动画；Web 继续先画 1000×560 不透明底图，桌面端再叠加对应地图的三层透明裁切。

自动验收已增加：十张地图三层资源逐图可加载、三种递归特效资源可加载、35 Hz / 35 FPS 约束仍通过。本轮已按用户要求只执行一次全局 Web 导出；浏览器入口、主菜单、地图选择页和错误日志检查通过。

### 已建立：P1 武器动画检查点

1. 中文和菜单兼容收尾已保存在 `ea9c613` 至 `4c47aac`。
2. 战斗反馈收口保存在 `518ec96`；完整 SWF 提取结论保存在 `3be0be8`。
3. 13 把箱内武器父级合成动画的代码、测试和文档已保存在 `e4a5dc5`。
4. 当前 Git 工作区干净；本地原始 SWF 和合成 PNG 继续受忽略规则保护，不进入公开分发历史。

### 当前主线：P2 玩法功能 Bug 全面收口

当前先关闭 BUG-010～021 及用户继续实机清点出的功能问题；完整强度 AI 是本阶段的一部分。AI 默认使用 Hard/无弱化强度，不以“能移动和开枪”的简化 AI 作为完成标准。阶段内按以下顺序推进：

| 子阶段 | 主要工作 | 阶段结果 |
| --- | --- | --- |
| ✅ AI-1 原版四槽位与控制基础 | 已按原版重做四个槽位的空置/玩家/AI 状态和按钮，并将角色键盘读取改为统一控制指令 | 四个槽位均可清空并重新选择 HUMAN 或 AI；任意一个非空即可开局，AI 不占用键盘按键 |
| ✅ AI-2 地图平台图 | 已从十张地图的 `Rect2` 平台数据生成平台节点与跳跃、下落连接；基础 AI 已使用下一平台中心追踪目标 | 十张地图均有可用路径和自动断言，不引入复杂导航引擎，也不改变原作粗糙碰撞 |
| ✅ AI-3 移动与救场 | 已实现追击、边缘刹车、跨平台跳跃、下落判断和被击飞后的回场优先级，并加入独立跳跃脉冲冷却 | AI 会沿平台图接近目标，不在无路线时直接走出边缘；出界或跌落时优先朝场内移动并使用剩余跳跃 |
| AI-4 完整战斗 | 先完成 BUG-012～021 的武器规则，再建立共享近战接敌控制器；AI 还需理解雨伞持续格挡、Sniper/Angry Cow/Mini Gun 前摇与松开射击、Sniper 隐身、炸弹安全距离、分裂弹以及飞刀普通远程/特殊近战分工；同时补齐二段/三段跳规划 | 五把默认武器和 13 把箱内武器均按最终规则使用；AI 不绕过前摇、冷却、弹药、隐身解除或跳跃次数 |
| AI-5 战术决策 | 加入威胁评估、弹道/爆炸回避、危险边缘换位和目标重新选择；每次出现可拾取武器箱时，AI 有 60% 概率将抢箱设为优先目标 | AI 不会固定追人或固定连射；抢箱决策按箱体事件抽取一次并保持到拾取、消失或危险条件中止，不得每帧重新随机 |
| AI-6 对局接入与调优 | AI 槽位自动准备；处理死亡、重生、胜负和目标失效；限制决策频率并调节随机性 | 1 名人类加 3 名 AI 可在固定 35 Hz 下完整结束 Free For All |
| AI-7 自动回归与收口 | 为控制输出、平台图、救场、武器选择、抢箱和完整对局增加测试；复核双人键盘模式不回退 | 主场景与冒烟测试通过，AI 长局无卡死、输入串线或持续自杀循环，并建立 Git 检查点 |

实现约束：AI 只输出与人类相同的左右、跳跃、下落、主攻击和副攻击指令，不直接修改位置、生命、弹药或命中结果；所有决策按固定 35 Hz 更新。Free For All 是本大步骤的完成范围，Team Deathmatch 与 Domination 只预留目标接口，不作为本轮阻塞项。

#### AI-1 四槽位交互规格

| 槽位状态 | 界面与行为 |
| --- | --- |
| 已有人类玩家 | 卡片顶部显示 `CLEAR SLOT / 清空槽位`；下面显示名称、角色预览和外观/武器/Perk 入口 |
| 已有 AI | 与人类卡片使用相同的清空和自定义结构，并额外显示红色 `AI PLAYER / AI 玩家` 标记 |
| 空槽位 | 原卡片位置保留，显示 `SLOT EMPTY / 槽位为空`、`SELECT A PLAYER TYPE / 选择玩家类型`，以及 `HUMAN / 玩家`、`AI` 两个大按钮 |
| 点击清空 | 仅清除对应槽位，立即切换为空槽位界面；不得影响其他槽位的类型、武器、Perk 或颜色 |
| 点击玩家 | 在原槽位生成一名人类玩家配置，恢复该槽位的完整人物卡片 |
| 点击 AI | 在原槽位生成一名完整强度 AI 配置，恢复完整人物卡片并显示 AI 标记 |
| 点击开始 | 至少一个槽位非空时即可开局；只为非空槽位创建角色和 HUD，AI 槽位自动参与，不要求键盘准备；四个槽位全空时才禁用开始 |

四个槽位都遵循同一状态规则，包括第一个槽位；不存在不可删除的固定主玩家。任意槽位都能独立执行删除、添加 HUMAN 或添加 AI，允许只保留任意一个人类或 AI 后开始，也允许删除到全空再重新配置。槽位类型与对局角色实例分离：菜单中保存配置，点击开始后再依据非空槽位创建对应人类或 AI 角色。界面布局、灰白卡片、粉红清空条、红色叉号和空槽位双按钮尽量参照用户提供的原版截图；不把现有“前两格固定、后两格预留”的行为继续作为最终实现。

## 6. P2 大步骤完成条件

- 四个槽位均能独立执行“清空 → 选择 HUMAN 或 AI → 生成对应卡片”，状态不会串到其他槽位。
- 任意一个槽位有人类或 AI 时即可开始；单角色配置必须能进入对局，只有四槽全空时不能开始。
- 玩家设置允许建立至少“P1 人类 + P2/P3/P4 AI”的 Free For All 对局，AI 槽位无需键盘准备；全 AI 配置可用于自动回归。
- AI 能选择有效敌人、跨平台追击、避免普通边缘失足，并在被击飞时合理使用剩余跳跃回场。
- AI 能按照当前武器特性使用主副攻击、处理弹药与换弹，并主动争抢武器箱。
- AI 普通追击时不连续乱跳；跳跃仅由跨平台、越障、高差追击或击飞救场等明确需求触发。
- AI 获得 Triple Jump 时能够按预计落点使用第二、第三次跳跃，并优先为高差路线或击飞救场保留额外跳数。
- 每个武器箱出现时，各 AI 有 60% 概率优先争抢；概率只在该箱出现时判定一次，目标在箱子拾取或消失后清除。
- AI 会对近距离弹丸、爆炸和高风险站位进行基础回避，目标死亡或重生后能重新决策。
- 1 名人类加 3 名 AI 以及 4 AI 自动对局均可结束，不出现长期原地卡住、持续自杀、攻击无效目标或输入串线。
- 双人键盘模式、35 Hz 节奏、现有武器逻辑、中文菜单和对局流程保持通过。
- 新武器动画人工验收继续保留为独立待办，不作为 P2 AI 完成条件。
- BUG-010～021 以及本轮后续登记的玩法功能 Bug 均已修复并通过针对性测试。
- P2 完成前只处理功能正确性和必要的可读性反馈；全面人物、武器、地图与 UI 重绘统一留到 P3。

## 6.1 下一大步骤：P3 全项目矢量重绘

P3 暂不执行，等待用户完成实机 Bug 清点并明确通知开始。目标是在不改变已冻结玩法的前提下，用 Godot 的 `Polygon2D`、`Line2D`、曲线和自定义绘制逐步替换研究版贴图：

1. 建立可切换的“参考贴图 / 矢量重绘”渲染层，先保证随时可以回退。
2. 重绘人物基础部件、颜色替换、帽子/脸/衣服和左右朝向。
3. 重绘五把默认武器和 13 把箱内武器，统一握点、枪口点、注册点与分层顺序。
4. 用部件变换和 35 帧关键姿势重建移动、跳跃、攻击、换弹、持枪和特殊动作。
5. 重绘弹丸、枪口、弹壳、命中、爆炸、拾取、状态效果和武器箱。
6. 重绘地图可见层、动态装饰、菜单与 HUD；碰撞和玩法数据保持冻结。
7. 对每个模块做原图叠加、左右镜像、两种玩家颜色、桌面/Web 截图和长局回归。

目标是尽量保持相同画面语言和构图，而不是声称与 Flash 渲染逐像素相同；Flash 与 Godot 的曲线抗锯齿差异单独记录。

## 7. 不得破坏的项目约束

- 玩法和显示保持固定 35 Hz/35 FPS，不改成 60 Hz，不启用物理插值。
- 保留原作允许的粗糙街机物理，不为偶发穿模引入复杂连续碰撞。
- 用户已经否决原创暖色工业荒漠美术；视觉方向继续参考 Redux 的粗线条 Flash 矢量卡通风格。
- `reference/original/` 和 `MayhemArena/assets/original_reference/` 只用于本地研究，继续保持 Git 忽略，不能作为公开发布资源。
- 不要删除或重置当前未提交改动；先阅读差异，再按功能拆分和提交。
- 完成一个里程碑后更新本文件，不要只在对话中口头报告。

## 8. 文档职责

| 文件 | 用途 | 更新时机 |
| --- | --- | --- |
| `STATUS.md` | 实时总进度、当前故障、测试结果、下一对话交接 | 每个工作阶段结束时 |
| `PROJECT.md` | 产品目标、技术选择、长期范围、里程碑和正式 Bug 条目 | 范围或里程碑变化时 |
| `REVERSE_ENGINEERING.md` | SWF 来源、符号、帧号、参数和接入证据 | 新逆向结论或映射变化时 |
